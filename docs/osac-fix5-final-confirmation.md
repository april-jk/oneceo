# OSAC 第三轮最终确认（进入交付）

日期：2026-02-18

我们确认采用你们建议：**控制面可用探测固定为**  
`GET_SESSION_LIST(maxCount=1, format=json)`。

要求如下：

1. Fix5 默认实现以该探测为准并写入交付文档。  
2. 若后续引入等价轻量探测，只能作为可选增强，默认不切换，并保持回归日志可对齐。  
3. 请按第二轮已锁定口径直接进入实现与交付。  

我们这边将以你们承诺的 4 条验收门槛做联调验收。  
本项确认后，**双方需求口径已一致，可结束需求对话，进入交付阶段**。
