# 2026-04-21 递归过程记录

- 做了什么：新增用户态设置中的“个性化”页签；扩展 `app_users.profile_json` 持久化用户级记忆；补齐 `PATCH /api/auth/profile`；侧边栏左下角入口默认打开个性化；账户页显示名保存改为真实接口。
- 遇到什么：仓库当前 worktree 很脏，且设置页原先只有假保存入口，没有现成 profile 存储结构。
- 计划如何解决：保持改动只落在 auth / settings 相关文件；通过 `app_users` 扩列而不是新建 profile 子表；用定向 auth 测试和双端 type-check 验证收口。
