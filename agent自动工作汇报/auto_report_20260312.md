## 2026-03-12

- 做了什么：
  - 阅读仓库协作规范、Agent 流程文档、当前 agent 架构文档，并核对了直通模式与 Altus 接管模式的现状实现。
  - 梳理了前端 `opencode_input` 发送链路、后端 `handleOpencodeInput` 消息入口，以及现有任务会话部署能力。
  - 新增“直通模式入口功能拦截层设计”文档，明确直通模式改为“一层入口判定 + 直通”，Altus 接管模式保持三层。
  - 已完成后端直通入口层第一版实现：新增能力拦截 Agent、直通入口服务、平台部署能力注册表，并在 `handleOpencodeInput` 中接入拦截流程。
  - 补充了架构文档同步说明与两组本地单元测试，用于覆盖“显式部署拦截”和“常规开发请求放行”。
- 遇到什么：
  - 用户描述里的“显式要求/显示要求”存在口语转写歧义，需要在设计中按“显式平台功能请求”做保守定义。
  - 当前平台能力种类较多，但适合作为 V1 拦截目标的已落地能力最明确的是部署相关能力。
- 计划如何解决：
  - 新增单元测试已通过；后续如要继续加固，再补直通模式端到端场景，验证“平台能力拦截后不再下发执行器”。
  - 已补充 Playwright 端到端回归，继续把“刷新一致性”和“执行器直通完成态”分开验证，逐项收敛剩余问题。

- 做了什么：
  - 使用 Playwright 补跑了直通模式下“发送部署状态查询请求 -> 入口层识别平台能力 -> 平台能力完成 -> 页面刷新”的完整链路。
  - 修复了刷新后历史回放会多出 `session_started/会话已创建` 的问题，保证实时渲染与历史重建渲染一致。
  - 修复了任务创建前端接口缺少 `X-User-Id` 请求头的问题，并让会话详情查询在 404 时安全返回，降低刷新噪音。
  - 优化侧栏最近会话列表：统一按 `updatedAt` 排序，并在会话页把当前会话固定到预览首位，消除刷新前后预览顺序抖动。
  - 新增 Playwright 用例 `apps/web/client/src/tests/direct-mode-refresh-consistency.playwright.spec.ts`，用真实像素对比验证刷新前后页面一致。
- 遇到什么：
  - 直通平台能力链路存在真实耗时波动，本地一次部署状态查询完成耗时约 22 秒，导致最初 20 秒的测试超时。
  - PNG 二进制和浏览器 canvas 解码都不适合作为稳定的截图一致性判断，前者会受压缩字节影响，后者会受颜色管理影响。
  - 普通开发请求的执行器直通链路在当前环境下仍未稳定完成，OpenCode 会话能启动并产生日志，但此前存在上游 `Unsupported request body.` 422 历史问题，当前这轮也未完成到最终成功态。
- 计划如何解决：
  - 继续单独排查执行器直通链路的上游兼容问题，把“消息已送达执行器”推进到“任务稳定完成并可刷新恢复”。
  - 若需要长期保留视觉回归，可把当前 Playwright 刷新一致性用例纳入固定回归集，作为直通模式改动的守护测试。
