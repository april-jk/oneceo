# 设置 / 域名

## 当前状态

该子页当前只保留前端 UI 占位，不进入具体功能设计。

后续如重新开启设置设计，再统一重写本页。

## 功能目标

统一承接部署工作台的域名与访问入口能力，包括：

- 默认访问地址
- 绑定域名列表
- 后续自定义域名接入

## 计划设计方式

### 当前阶段

- 展示主访问地址与可访问入口
- 不开放自定义域名写操作

### 后续阶段

- 新增域名绑定流程
- 增加 DNS 校验与生效状态
- 区分默认域名、自定义域名、预览域名

## 当前建议的数据来源

- `domains`
- `latestUrl`
- `latestStaticUrl`
- 当前 deployment 的 url/staticUrl

## 审核点

- 当前是否确认只保留 UI 占位。
