# 设置 / 密钥

## 当前状态

该子页当前只保留前端 UI 占位，不进入具体功能设计。

后续如重新开启设置设计，再统一重写本页。

## 功能目标

承接部署相关密钥与环境变量管理。

## 计划设计方式

### 当前阶段

- 展示平台托管部署凭据已就绪或准备中的状态
- 不向用户暴露供应链 token

### 后续阶段

- 支持用户自定义环境变量
- 支持多环境变量作用域
- 支持审计与轮换

## 当前建议的数据来源

- `configured`
- `missing`
- 后续新增的用户环境变量配置模型

## 审核点

- 当前是否确认只保留 UI 占位。
