## 2026-03-15

- 做了什么：
  - 继续优化 `tools/service_manager_gui/service_manager_gui.py` 的交互阻塞问题，把服务按钮、批量按钮、端口强杀按钮改成“发送后台指令即返回”，不再在执行期间把按钮置灰。
  - 关闭默认自动日志刷新，只保留手动刷新和操作完成后的按需刷新，避免刷新按钮持续进入占用状态。
  - 将状态自动刷新间隔上调到 5 秒，并把异步错误/冲突提示从阻塞式弹窗改为底部状态栏提示。
  - 更新 `docs/20260308_service_manager_gui.md` 与工具 README，记录新的非阻塞交互策略。
- 遇到什么：
  - 之前虽然动作执行已在后台线程，但 UI 仍通过 busy 状态和日志自动刷新把“刷新按钮正在占用”的感知暴露给用户。
  - Tkinter 的模态对话框会进一步放大“按钮点不动”的体感，不适合这种后台指令式工具。
- 计划如何解决：
  - 先继续做最小验证，确认脚本可编译、`--self-check` 正常。
  - 如果用户继续反馈卡顿，再进一步把状态采样从共享线程池切到专门的后台调度线程。

## 2026-03-15 21:32 CST

- 做了什么：
  - 完成 task session 对话区“最近 50 条热缓存 + 上滑增量历史 + 刷新后进度保持”的主链路修复。
  - 后端保留最近 50 条消息热缓存，前端接入 `/messages/recent` 和 `/messages/history`。
  - 修复了 older history prepend 后滚动位置丢失的问题，增加前端本地重入锁，避免重复 `scroll` 事件把恢复锚点清空。
  - 修复了历史已展开时，SSE replay / recent reload 覆盖当前已展开历史的问题。
  - 用 Playwright 做了前台回归，验证了“首屏立即显示 recent cache、上滑继续看历史、刷新后保持查看进度”。
- 遇到什么：
  - React 开发模式下存在 StrictMode / HMR 噪音，会产生重复 recent 请求和额外控制台噪音，干扰问题判断。
  - `scroll` 事件会在一次用户上滑过程中重入多次，第一次分页未完成时，第二次事件会把 prepend 恢复锚点提前清空。
- 计划如何解决：
  - 保留当前实现，后续在生产模式再补一轮验证，确认没有开发模式额外噪音。
  - 如果后续仍发现极端长会话下滚动抖动，再继续优化 recent merge 与 live tail 的并发合并策略。
