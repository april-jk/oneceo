## 2026-03-09

- 先根据报错定位到两个独立问题：`markSandboxDirty` 重复导出导致 esbuild transform 失败；API 热重启时旧进程可能未及时退出，造成 `EADDRINUSE: 4000`。
- 读取 `sandbox-activity-service.ts` 后确认文件里确实存在两个同名导出，于是保留增强版实现，并把旧实现里的 `lastActiveAt / lastActiveReason` 更新语义合并进去。
- 接着检查 `apps/api/src/index.ts`，发现入口没有明显的 `SIGTERM / SIGINT` 收口，因此补充了 `httpServer.close()`、`socket.io close()`、`taskCreationWebSocketService.close()`、归档任务停止和数据库关闭。
- 同时在 `apps/api/src/agents/task-creation/websocket-service.ts` 上挂 `error` 监听，避免 WebSocketServer 在底层监听异常时再触发未处理事件。
- 最后用带 `apps/.env` 的 `tsx` 导入验证模块编译通过，再用 `PORT=4101` 启动一次 API 并发送 `SIGTERM`，确认服务可以正常启动并优雅退出。

## 2026-03-09 补充

- 用户补充说明“问题不在提交位置，而是在 Mac 上第二条消息无法继续交互”，于是把排查重心从 git diff 转到运行态。
- 先检查本机正在运行的 Web/API，再直接执行 `tests/opencode-sandbox-direct` 的场景 01 和场景 02；结果显示后端直通链路在这台 Mac 上是通的，第二条消息可以继续落到同一个 OpenCode 会话。
- 接着使用浏览器自动化在 `http://127.0.0.1:3000` 实际发送消息，验证第一、第二、第三条都能继续执行，说明前端主流程在 Chromium 里也能通过。
- 在浏览器 Network/Console 里发现一个更像“跨浏览器偶发问题”的点：首次请求 `/api/task-creation/sessions/:id/opencode/events` 时会出现 `404`，同时前端原本在 `openSse()` 一开始就把 `ssePreferredRef` 置为 `true`，SSE 失败后也不立即回退。
- 这会让某些浏览器更依赖 SSE 的实现细节时，更容易表现成“第二条消息发出后没有继续执行”。因此补了两个修正：
- `task-creation-routes.ts` 的 SSE 路由改为通过 `resolveTaskSessionRecord()` 读取会话，减少首连 404。
- `useTaskCreationAgent.ts` 中把 `ssePreferredRef` 改为仅在 `source.onopen` 后置 `true`，并在 `source.onerror`/`closeSse()` 时回退到 `false`，确保 WS 能稳定兜底。

## 2026-03-09 再补充

- 用户给出新的稳定复现输入：
  - 第一条：`帮我开发工程备忘录软件`
  - 第二条：`使用nodejs + sqlite开发，创建/编辑/删除备忘录功能就可以，然后本地存储就行`
- 用这组输入重新观察后发现：
  - 第一条会在 OpenCode 内部触发 `question` 工具，并产生 `question.asked`。
  - 页面上虽然会显示“待确认”卡片，但前端发送第二条时走的仍是普通 `opencode_input`。
  - 结果是第二条消息被发到 `/session/{id}/message`，OpenCode 只把它当成新的用户消息回显，没有把它当作挂起问题的回答。
- 进一步探测 OpenCode 远端后确认：
  - 官方就有 `GET /question` 和 `POST /question/{requestID}/reply` 两个接口。
  - `reply` 的 `answers` 参数要求是 `string[][]`，不是简单字符串数组。
- 因此这轮修复不再继续改浏览器状态机，而是把重点放到 API 桥接：
  - 先查当前 opencode session 是否存在挂起 `question`。
  - 如果存在，则把用户的自然语言补充映射成 `answers[][]`，并调用 `reply`。
  - 回复前把本地 task session 从 `completed` 重开到 `in_progress/executing`，避免续跑时被旧终态卡住。
- 验证结果：
  - 新增的 `opencode-question-adapter` 单测已通过。
  - `type-check` 仍有仓库内历史错误，但新加逻辑没有再引入额外类型错误。
  - 完整 smoke 仍会受到本机旧 API 进程未热更新、以及新建 E2B runtime 偶发 `fetch failed` 的环境噪声影响，因此后续以重启后的当前 API 继续复测为准。

## 2026-03-09 最终复测

- 先用 Playwright 在页面里把首条 `帮我开发工程备忘录软件` 发出去，等待 OpenCode `question` 卡片出现，再发第二条 `使用nodejs + sqlite开发，创建/编辑/删除备忘录功能就可以，然后本地存储就行`。
- 第一轮页面已经能继续执行，但从持久化元数据里看到 `questionAnswers` 被映射成了 `["创建和编辑备忘录", "协作功能"]`，说明“继续执行”问题已经解决，但选项适配还存在误命中。
- 检查后发现旧 API 进程不是 `tsx watch`，而是凌晨留下的 `tsx src/index.ts`，因此页面实际跑的还是旧逻辑。
- 将 `opencode-question-adapter.ts` 的匹配策略改为：
  - 主选项命中权重高于描述命中；
  - 去掉“功能/应用/工具/软件”等通用后缀后再做片段匹配；
  - 多选题只保留强匹配项，避免“编辑”把“协作功能”一起带上。
- 补充回归测试后重启 API 为 `pnpm dev`，整页刷新浏览器再复测。
- 最终会话 `fd452de7-eb58-4b33-99d0-930d07b1e7c8` 的结果：
  - 第二条消息继续执行成功；
  - `answeredVia` 为 `opencode_question_reply`；
  - `questionAnswers` 变为 `[[原始文本], ["创建和编辑备忘录"], ["JavaScript/TypeScript"]]`，不再误选“协作功能”。

## 2026-03-09 连接器设置页重构

- 用户希望“设置 -> Connectors”更像连接器目录页，一级只看列表，详细配置下沉到下一层。
- 读取 `apps/web/client/src/components/ConnectorCenterPanel.tsx` 后确认原实现把 quick guide、manual config、OAuth、attach session 都直接堆在一级，导致页面很长且和参考样式差异很大。
- 于是将组件改成两层结构：
  - 默认页：连接器列表行，展示图标、名称、描述、状态、右箭头和底部“添加连接器”按钮；
  - 详情页：返回按钮 + 当前连接器完整配置、引导、错误、授权和挂载操作。
- 代码通过了 `pnpm --dir apps/web exec tsc --noEmit`。
- 用 Playwright 实际打开设置弹窗确认：
  - 一级列表页已经是新的行式布局；
  - 点击 GitHub 行后会进入二级详情页，显示“返回连接器列表”和详细配置区域。
- 中间遇到一个环境点：`3000` 上原来的 Vite 进程没有把新组件热更新给浏览器，需要手动重启 web dev 进程后再看实际页面。

## 2026-03-09 连接器详情改为弹窗

- 用户进一步要求详情不要再递归到更深一层子窗口，而是用额外弹窗展示。
- 因此把 `ConnectorCenterPanel.tsx` 的详情交互改为：
  - 一级列表常驻；
  - `selectedKey` 仅用于控制详情 `Dialog` 的开关；
  - 点击某一行后，在列表上方打开详情弹窗；
  - 关闭详情弹窗后，列表保持原位。
- 再次验证：
  - `pnpm --dir apps/web exec tsc --noEmit` 通过；
  - Playwright 实际点击 GitHub 行后，会打开独立的详情弹窗；
  - 关闭详情弹窗后，会回到连接器列表。

## 2026-03-09 附件加号菜单递归记录

- 先确认“加号”入口统一收口在 `AttachmentPickerButton.tsx`，避免在各个页面各自拼菜单。
- 远程导入没有现成 API，先抽出 `remote-attachment-service.ts`，把网址安全校验、Google Drive/OneDrive 分享链接转换和文件名推断独立出来，便于单测。
- 第一轮 UI 实测发现网站导入后名称退化成 `website-file`，继续顺着网络层看，定位到自定义响应头没被 `Access-Control-Expose-Headers` 暴露。
- 在 API 路由补暴露头后重新测，远程导入恢复为 `logo.png`。
- “使用技能”这轮没有直接接 OSAC skill 管理，因为 E2B 路径当前明确不支持；先落成 skill brief 附件，把交互需求闭环。
- Playwright MCP 在 file chooser 场景里 transport 断开，后续切到本地 `@playwright/test` 跑 smoke，最终把网站导入、技能导入和本地导入都跑通了。

## 2026-03-09 GitHub 连接器递归记录

- 先读了连接器中心前端、用户连接器保存服务、会话挂载服务和热更新设计文档，确认 GitHub 连接器是用户级保存 token，再在会话级 attach 到 runtime。
- 排查时先怀疑官方 MCP server 的环境变量名不匹配，核对后确认 `GITHUB_PERSONAL_ACCESS_TOKEN` 本身没有问题。
- 接着发现更像问题的点有两个：
  - 后端保存 GitHub PAT 时不会先校验 token，理论上可能把无效 token 直接标成 `authorized`；
  - 设置页带 `targetSessionId` 时，保存成功后如果自动 attach 失败，前端会把整个流程视为失败，导致用户误以为“连配置都没生效”。
- 用用户给的 PAT 直连 GitHub `/user` 验证成功，账号为 `april-jk`，说明用户给的 token 本身有效。
- 因此实施两处修复：
  - 保存 GitHub 连接器前调用 GitHub `/user` 做校验并回填 displayName；
  - 前端把“保存”和“挂载”拆开，挂载失败时仍刷新已保存状态，并明确提示“已保存，但挂载失败”。
- 然后补了 API 单测，覆盖：
  - 有效 token 会保存并带回 `april-jk`；
  - 无效 token 会直接拒绝且不会落库。
- 最后用本机运行中的 API 做了临时用户 `codex-github-connector-test` 的接口验证：
  - `PUT /api/connectors/github` 返回 `authorized`；
  - `GET /api/connectors/me` 能读回 `april-jk` 和密文摘要；
  - 验证后执行 `DELETE /api/connectors/github/auth` 清理测试数据。

## 2026-03-09 GitHub MCP 假成功递归记录

- 先用 Playwright 在全新 task session `8a07d793-1a12-44a9-b5a5-537e3e9cfdec` 上重新启用 GitHub 连接器，排除旧 session 脏状态。
- 发现前端会显示已启用，但运行中智能体仍然只调用 `bash`，先后执行 `gh auth status`、`env | grep -i github` 等命令，说明 MCP 工具没有真正进入可用态。
- 顺着 attach 路由往下抓到关键矛盾：
  - `POST /mcp` 返回体里有 `github--8a07...: connected`；
  - `POST /mcp/:name/connect` 返回 `true`；
  - 但同一个 baseUrl `https://4096-ia5x5pxnljeli3rnp98bi.e2b.app` 上，紧接着 `GET /mcp` 永远只返回 `{\"playwright\":{\"status\":\"connected\"}}`。
- 为避免 shell 包装干扰，改成直接 `['node', '-e', script]` 启动 GitHub MCP；并用最小复现再次验证：
  - 直接向 runtime `POST /mcp` 注册临时 `github-ephemeral-check` 时，响应体显示已 `connected`；
  - 随后立即 `GET /mcp`，临时 server 和 GitHub server 都消失；
  - 证明问题不在前端，也不在 attach 路由，而在 OpenCode runtime 对新增 MCP server 的保留行为。
- 最后把 attach 逻辑改成“注册成功后必须回读 `/mcp`”，如果 server 没留下来，就把会话连接器状态写成 `failed`，错误文案为“运行时未保留已注册的 MCP 服务”。

## 2026-03-09 GitHub MCP 注入递归记录

- 把会话 `8a07d793-1a12-44a9-b5a5-537e3e9cfdec` 重新 attach 后，远端 `/mcp` 已稳定返回：
  - `playwright: connected`
  - `github--8a07d793-1a12-44a9-b5a5-537e3e9cfdec: connected`
- 继续直接读 sandbox 内部状态确认：
  - `~/.config/opencode/opencode.json` 已包含 `github--8a07...` 的 MCP 配置；
  - `opencode serve` 进程环境里已有 `GITHUB_PERSONAL_ACCESS_TOKEN`、`GH_TOKEN`、`GITHUB_TOKEN`。
- 这说明“把 GitHub 连接器直接配置到远端 sandbox 的 OpenCode 中，并且不需要重新鉴权”这一层已经成立。
- 新的失败点转移到了模型请求层：
  - Playwright 发送新指令后，OpenCode 的 `session.error` 返回上游 `chat/completions` 400；
  - 具体错误是“请求格式非法，请检查请求结构是否符合 API 规范”；
  - 因而当前未能继续完成“创建远程仓库并推送”的终局验证。
- 另外复测时顺手发现：
  - `POST /api/task-creation/sessions` 之前会默认生成 `session_...` 风格 ID；
  - 但数据库 `task_creation_sessions.id` 是 UUID，导致新草稿会话无法落库、无法 attach；
  - 已改为默认生成 UUID，并在创建 DB session 时同时绑定当前用户。
