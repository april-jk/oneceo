# 20260411 调试工具化与 n.eko 页面控制链路方案 [20260411-2310已采用]

## 1. 背景

当前“启动网站调试功能”主要通过自然语言触发，缺少独立的工具语义：

1. 模型可能把“调试”理解为普通命令执行，无法稳定走统一调试链路。
2. 无法表达“先启动服务，再让 n.eko 浏览器打开目标页面”的可组合工具流程。
3. 调试动作在事件流里缺少稳定工具名，不利于观测与复盘。

你提出的目标是：参照 Suna 的工具化思路，把调试能力做成可被模型准确识别、可重复调用、可与其他工具协同的专用工具。

## 2. 参照 Suna 的实现依据

本方案直接参考 `referance/suna/backend/core/tools/browser_tool.py` 的三点设计思想：

1. 能力以独立工具注册（不是提示词临时约定）。
2. 工具职责清晰、参数结构化、可多次调用。
3. 工具可与其他工具串联（例如先操作环境，再做浏览器动作）。

本方案不照搬 Suna 的 Stagehand 浏览器栈，只吸收“工具化与编排”模式；实际执行仍使用 oneceo 现有 E2B + OSAC + n.eko 链路。

## 3. 目标与非目标

### 3.1 目标

1. 新增 Altus managed 专用工具 `debug_open_page`，让模型通过工具调用而不是自由文本命令触发调试链路。
2. 支持“非一次性调用”：同一 run 内可多次调用，并与 `shell_execute` 等工具组合。
3. 工具执行统一复用现有 `ensureNekoDebug`，并通过 Chromium CDP 打开指定页面。
4. 在 run 事件流中稳定记录 `tool_call_started/completed` 的工具名 `debug_open_page`，便于排障。

### 3.2 非目标

1. 不新增第二套调试服务端口或新 sandbox 调试架构。
2. 不改 `kvm-orchestrator` 主链路（继续停用）。
3. 不引入 Suna Stagehand/browser API 依赖。

## 4. 方案设计（最短路径）

### 4.1 新增工具定义

在 `apps/api/src/services/altus-managed-shared.ts` 的 `buildManagedToolDefinitions()` 增加：

1. 工具名：`debug_open_page`
2. 参数：
   - `url`（string，必填）：要在 sandbox 内 Chromium 打开的 URL（支持 `http`/`https`，可含路径、查询参数与锚点）。
     - 示例1：`http://127.0.0.1:3000`
     - 示例2：`http://127.0.0.1:3000/folder1/`
     - 示例3：`http://127.0.0.1:3000/folder1/index.html`
     - 示例4：`http://127.0.0.1:3000/app?tab=debug`
     - 示例5：`http://127.0.0.1:3000/app?tab=debug#section-2`
     - 示例6：`https://example.com/docs/getting-started`
   - `ensureDebug`（boolean，选填，默认 `true`）：调用前是否确保 n.eko 调试已就绪。
3. 描述中明确关键短语：`启动网站调试功能`、`打开调试页面`、`在 n.eko 中打开页面`。

### 4.2 工具运行时执行逻辑

在 `apps/api/src/services/altus-managed-tool-runtime.ts` 的 `execute()` 中新增 `debug_open_page` 分支：

1. 参数校验：`url` 必须为非空字符串，且必须是 `http://` 或 `https://`。
   - 保留并透传完整 path/query/hash（例如 `/folder1/`、`?tab=debug`、`#section-2`），不做路径截断与重写。
   - 若用户仅提供主机或缺协议（如 `127.0.0.1:3000/folder1/`），返回明确错误，提示补全为 `http://127.0.0.1:3000/folder1/`。
2. 当 `ensureDebug=true` 时：
   - 调用 `ensureNekoDebug(this.input.sandboxId)`，复用既有调试启动链路。
3. 用 `e2bConnector.runCommand` 通过 Chromium CDP 打开页面：
   - 优先调用 `PUT http://127.0.0.1:${cdpPort}/json/new?{encodedUrl}`。
   - 若失败，回退一次 `GET` 方式（兼容不同 Chromium 行为）。
4. 返回结构化结果（`type: result`）：
   - `targetUrl`
   - `debugUrl`（n.eko 访问地址）
   - `ready`
   - `status`
   - `sandboxId`
   - `cdpPort`
5. 该工具执行后调用 `markWorkspaceDirty('managed_debug_open_page')`，与现有运行时脏标记机制保持一致。

### 4.3 提示词与识别规则

在 `apps/api/src/services/altus-managed-prompt-service.ts` 的 `# Tool usage rules` 增加明确规则：

1. 当用户要求“启动网站调试功能 / 打开调试页面 / 在调试窗口打开某网站”时，优先调用 `debug_open_page`。
2. 若目标站点未运行，先用 `shell_execute` 启动服务；`shell_execute` 对常驻预览/dev 服务使用受控后台服务模式，返回 `service.url` 后再调用 `debug_open_page`。
3. 禁止通过临时 `docker-compose/install.sh` 文本命令替代该工具链路。
4. sandbox 默认预装 Browser Use CLI、Playwright 与 `@playwright/mcp`。
5. 调试、自动化测试、回归验证、截图验收和本地应用检查默认使用 Playwright / `playwright-mcp`，并且必须连接 n.eko 正在回传的同一个 Chromium CDP 端口（默认 `http://127.0.0.1:9222`），不要启动独立浏览器。
6. `browser-use` 仅作为外部网站探索、导航、点击、表单填写和页面探查的辅助能力；如果需要复用调试 Chromium，必须显式使用 `--cdp-url http://127.0.0.1:9222`。
7. 对 workspace 内已生成的 standalone HTML 交付物，允许 `debug_open_page` 直接打开 workspace 内的 `file://` URL；该能力只允许 workspace 内路径，禁止打开 workspace 外文件。
8. 不允许在任务运行时把 Browser Use、Playwright 或 `@playwright/mcp` 安装进用户项目；如果 sandbox 缺失这些能力，应报告 sandbox 能力异常。
9. `debug_open_page` 只负责打开页面；打开后的功能验证必须用 `browser_interact` 记录具体浏览器动作。
10. `browser_interact` 只能暴露 Playwright 已支持的受控动作投影：`locator_click` -> `locator.click`、`text_click` -> `getByText(...).click`、`coordinate_click` -> `mouse.click`、`locator_fill` -> `locator.fill`、`keyboard_type` -> `keyboard.type`、`keyboard_press` -> `keyboard.press`、`mouse_wheel` -> `mouse.wheel`、`wait_for_locator` -> `locator.waitFor`、`wait_for_text` -> `getByText(...).waitFor`、`wait_for_load_state` -> `page.waitForLoadState`、`wait_for_timeout` -> `page.waitForTimeout`。
11. `browser_interact.description` 必须写成人能理解的动作说明，例如 `点击“新游戏”按钮`、`按下 ArrowUp 键`、`向下滚动页面`，禁止继续使用“这一步完成”“我继续处理”这类无法判断动作含义的描述。

这保证“文本意图 -> 专用工具”映射稳定，不依赖偶然 prompt 理解。

### 4.3.1 调试触发后的测试流程约束

“启动网站调试功能”不能只理解为把页面打开给用户看，而应进入交付前测试流程：

1. 首次调用 `debug_open_page` 前，Altus 必须先在 workspace 编写或更新测试文档，例如 `docs/test-plan.md`。
2. 测试文档至少包含：需求摘要、目标用户流程、测试用例、验收标准、执行结果、发现问题、修复后复测结果。
3. 创建测试文档后，Altus 通过 `todowrite` / 进度消息进入 testing 阶段，再启动或打开目标应用。
4. `debug_open_page` 成功后，必须使用 Playwright / `playwright-mcp` 连接同一个 n.eko Chromium CDP 9222 做功能测试。
5. 测试过程中的用户动作必须通过 `browser_interact` 逐步暴露到时间线：打开网页、点击控件、按下键盘按键、输入文本、滚动页面、等待元素/文本或等待页面加载状态。
6. 测试范围不能只看页面是否可达，必须覆盖需求中的核心功能流、可见内容、导航、关键控件/表单/交互、状态变化、响应式布局（如适用）和明显运行时错误。
7. 如果测试发现问题，Altus 必须把失败记录进测试文档，进入 repair，使用文件/代码工具修复，再重新执行相关测试并更新复测结果。
8. 只有测试文档中关键用例通过，且 Playwright 验证不是 `about:blank`、Chrome 错误页或错误路由后，才能 `complete_task`。

### 4.4 与前端触发入口的关系

前端保留现有“执行环境未启动”按钮发送固定文本 `启动网站调试功能` 的行为，不新增前端协议。

后端通过新工具定义 + 提示词规则完成稳定识别并落到调试工具链路。

## 5. 典型编排流程（非一次性）

用户意图：启动本地站点并在调试画面打开。

1. `shell_execute`：`pnpm dev --host 0.0.0.0 --port 3000`（或项目实际启动命令）。
2. `shell_execute` 返回 `runMode=background_service`、`service.url=http://127.0.0.1:3000/`、`service.logPath` 和 `service.pid`。
3. `debug_open_page`：`url=http://127.0.0.1:3000`。
4. 若页面路由切换，再次调用 `debug_open_page`：`url=http://127.0.0.1:3000/admin`。
5. 如果调试对象是 workspace 内 standalone HTML 文件，可直接调用 `debug_open_page`：`url=file:///opt/.altus/opencode/workspaces/<sessionId>/index.html`。
6. `browser_interact`：`action=text_click, text=新游戏, description=点击“新游戏”按钮`。
7. `browser_interact`：`action=keyboard_press, key=ArrowUp, description=按下 ArrowUp 键`。
8. `browser_interact`：`action=wait_for_text, text=2048, description=等待页面出现“2048”`。
9. 如果调试对象是外部网站，优先先用 Playwright 连接同一个 n.eko Chromium 做可视检查；只有在需要更高层的探索式导航、点击或表单交互时，才使用 `shell_execute` 调用 `browser-use open/state/click/type/screenshot`，并显式复用 CDP。
10. 如果调试目标是本地应用质量验收，使用 Playwright / `playwright-mcp` 执行确定性测试并输出测试结果。
11. `debug_open_page` 成功前必须完成目标 URL 可达性校验 / workspace 文件可读性校验和 CDP tab 就绪校验；若目标服务未启动、返回 5xx、文件不存在或 tab 未出现，工具返回失败，Altus 必须继续修复并重新调用工具，不能对用户回显“页面已打开”。
12. `debug_open_page` 的校验脚本不能依赖非零退出码承载业务失败原因；E2B 可能只返回 `exit status 1`。脚本必须以结构化标记输出失败类型，并由运行时转换成可恢复错误。
13. `debug_open_page` 成功只代表目标 URL 已打开到 n.eko 的 Chromium tab；“页面正确显示”必须再由 Playwright 检查同一个浏览器中的页面标题、URL、DOM 或截图，确认不是 `about:blank`、Chrome 错误页或错误路由。
14. 调试触发即进入测试流程：先写 `docs/test-plan.md`，再打开调试页并执行 Playwright 功能测试；发现缺陷后修复并复测。

以上过程中 Altus 可继续调用 `read_file/search_code/shell_execute` 等工具，不会因调试工具调用而终止流程。

## 6. 涉及文件清单

1. `apps/api/src/services/altus-managed-shared.ts`
2. `apps/api/src/services/altus-managed-tool-runtime.ts`
3. `apps/api/src/services/altus-managed-prompt-service.ts`
4. `apps/api/tests/altus-managed-tool-runtime.test.ts`
5. `apps/api/src/services/sandbox-agent-provision-service.ts`
6. `e2b_templates/opencode-playwright-mcp/template.ts`
7. `e2b_templates/codex-ws-playwright-sandbox/template.ts`

## 7. 验证方案

1. 单测：
   - `debug_open_page` 参数校验（空 URL / 非 http(s) URL）。
   - `ensureDebug=true` 时调用 `ensureNekoDebug`。
   - 目标 URL 不可达或返回异常状态时，`debug_open_page` 失败且不标记工作区调试成功。
   - CDP 打开成功时返回结构化结果。
   - managed prompt 明确调试/测试默认 Playwright 连接同一个 n.eko Chromium，Browser Use 仅作为外部网站探索式交互辅助能力。
   - managed prompt 明确调试触发前必须写测试文档，并在失败时进入 repair + retest。
   - task intent 将“启动网站调试功能”识别为 `debug_chain`，确保进入 todo/testing 工作流。
2. 类型检查：
   - `pnpm --filter api type-check`
3. 最小链路联调：
   - 发送“启动网站调试功能”；
   - 观察 run 事件中出现 `tool_call_started/debug_open_page`；
   - 调试页可打开目标 URL。
4. sandbox 能力验收：
   - provision 中 `browser_use_present` 通过；
   - `sandbox_verify` 输出 `browser_use=present`；
   - sandbox 内 `browser-use --help` 与 `playwright --version` 均可执行。

## 8. 风险与约束

1. Chromium CDP `/json/new` 在不同版本可能存在方法差异（PUT/GET），因此在工具内部一次性兼容两种请求方式。
2. 若 sandbox 内目标服务未启动，`debug_open_page` 会在 URL 探测阶段失败；它不负责猜测业务启动命令，应由 Altus 先调用 `shell_execute` 启动服务并重新验证。
3. Browser Use 是外部网站探索/交互辅助工具，不是回归测试替代品；调试和测试类任务仍必须默认走 Playwright。
4. 新模板需要重新构建并配置到运行环境后，旧 sandbox 才会逐步淘汰；运行时 provision 会通过 `browser_use_present` 尽早暴露模板滞后问题。

## 9. 评审结论

1. 工具名采用：`debug_open_page`。
2. URL 协议约束采用：仅 `http/https`（调试场景允许 `http`，不开放 file/data 等协议）。
3. 本方案已确认进入实现，文档状态更新为 `[20260411-2310已采用]`。
