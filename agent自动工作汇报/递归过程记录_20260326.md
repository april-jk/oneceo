# 2026-03-26 递归过程记录

- 做了什么：梳理了项目内现有 skill 入口，确认当前实际可用的是 `apps/web` 的 skill 附件模板，而不是 OSAC 真正的 skill 热加载；同步参考了 GitHub / 技能站点上的 presentation skills 设计。
- 遇到什么：`apps/api/src/services/osac-agent-service.ts` 在 E2B 模式下对 `LOAD_SKILL` / `UNLOAD_SKILL` 直接报“不支持”，所以不能把这次 PPT skill 当成 sandbox 热加载功能去实现。
- 计划如何解决：先沿用现有 skill 附件链路，新增一个 `PPT 办公` 内置模板，并把模板源独立出来，后续如需“真正安装 skill 到 sandbox”再单独走第二阶段能力建设。
- 新增记录：用户随后指定参照 `MiniMax-AI/skills` 的 office 相关 skill 做改进，因此把能力范围从单个 PPT 模板扩到 Office 三件套模板，并把模板内容改成更接近真实办公工作流的分流式指令。
