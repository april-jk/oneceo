## 2026-03-08

- 第一轮先确认运行时边界，复用现有 OpenCode `/mcp` API，不改 E2B 模板。
- 第二轮补后端基础设施：身份解析、密文存储、连接器注册表、用户级配置与会话级绑定。
- 第三轮把 `ensureOpencodeServer -> reconcile` 和 SSE 活跃状态更新接入主流程。
- 第四轮接前端：统一身份头、连接器中心、当前会话弹窗、设置入口，并把连接器中心收口到“设置-连接器”。
- 第四轮半继续打磨设置页：给 4 个连接器补上官方站点直达、token/key 获取步骤、DSN 模板和 OAuth/手动配置并存入口。
- 第四轮后三分之二重做 Session Connectors 弹层：从大卡片改成紧凑列表和开关式挂载，底部保留添加/管理连接器入口。
- 第四轮最后又把交互再收紧：连接器按钮改成按钮旁一级浮窗，行点击打开二级配置浮窗，添加/管理才进入设置弹窗。
- 第四轮收尾补了一次边界修复：一级和二级浮窗都按当前可用视口自动收缩，防止弹层超出显示区。
- 第四轮继续修一级浮窗内部布局：把首层状态文案全部收掉，只保留名称和开关，并将弹层改成三段式结构，中部连接器列表独立滚动，底部“添加连接器/管理连接器”固定。
- 第四轮再补一个细节：一级列表左侧统一加连接器品牌图标，首层最终收口成“图标 + 名称 + 开关”。
- 第四轮后半把 `Add attachment` 真正做通：前端新增附件选择按钮和附件条，根页选择的附件会跟着跳进 `/new-task`，Home 在首次带附件发送时先建 draft session，再把文件传到 workspace 的 `.attachments/`，最后把附件清单挂到消息 metadata。
- 第四轮继续扫尾附件入口：把 `NewTaskDialog` 和 `AIAgent` 两个残留死按钮也替换成统一附件选择器，并让 `NewTaskDialog` 的首条输入支持附件上传后再发送。
- 第五轮处理运行时报错：发现 `user_connector_accounts` 查询失败来自新表未在现有库中创建，于是把连接器表迁移拆成可单独执行的 bootstrap，并在 API 启动与连接器服务入口统一兜底。
- 最后一轮补文档和单测，并分别跑前端 `tsc` 与新增测试。
- 新开一轮做本地服务管理 GUI：先读 `02_services.md` 和根目录 `start/stop-*.sh`，确认四个服务与端口约定。
- 之后用 Python + Tkinter 单独实现 GUI，不改现有业务模块，控制逻辑沿用仓库既有端口和 `/tmp/oneceo-mac*` 状态目录。
- 中途发现 PyInstaller 在当前 Python 环境下被旧 `pathlib` 包卡住，于是改成工具目录本地 venv 打包。
- 打包首次成功但产物误落到仓库根目录，随后修正 `build-mac.sh` 的 `--distpath/--workpath/--specpath`，重新生成到 `tools/service_manager_gui/dist/`。
- 最后一轮用源码和打包后二进制分别跑 `--self-check`，确认仓库解析和服务状态探测可用。
