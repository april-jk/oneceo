# OSAC 联调记录（交付给 OSAC 团队）

## 基本信息
- 日期时间: 2026-02-08
- session_id: sess_e747d935d97549bb
- VM IP（原始）: 172.28.0.172:18080
- 端口映射（转发）: 192.168.10.172:21399（当前不可达）
- OSAC token（本次测试用）: 0d09c176e5074bfc95275acb4a760e48

## 现象与结论
1) OSAC 可以启动并监听 18080
- VM 内 `ss -ltnp | grep 18080` 显示 OSAC 进程监听
- `osac.log` 有 "OSAC listening on :18080" 记录

2) OSAC 执行部分 opencode 命令可返回输出
- 通过 OSAC WebSocket 发送 `EXECUTE_COMMAND`，命令 `debug config`
- 收到多条 `COMMAND_OUTPUT` 片段，并 `COMMAND_STATUS: completed`

3) GET_SESSION_LIST / session list 命令异常
- 通过 WebSocket 发送 `GET_SESSION_LIST` 时：
  - 返回 `ERROR`：`SESSION_LIST_PARSE` / `unexpected end of JSON input`
- 通过 `EXECUTE_COMMAND` 发送命令 `session list --format json --max-count 5`：
  - 收到 `COMMAND_STATUS: running`，随后长时间无 completion（或输出为空后 completed）
- 预期：应返回 JSON 数组（即使为空也应输出 `[]`）

4) 直接在 VM 内执行 opencode（guest-exec）时可能阻塞
- `opencode session list --format json` / `opencode debug config` 通过 QGA guest-exec 执行时发生超时
- 但 OSAC 中 `debug config` 能返回输出，说明 OSAC 可能做了额外处理（env/flags）

## 复现步骤（OSAC 侧）
WebSocket：`ws://192.168.10.172:21399/ws`
Auth：`Authorization: Bearer <OSAC_AUTH_TOKEN>`

### A. EXECUTE_COMMAND（debug config）
```json
{ "type": "EXECUTE_COMMAND", "payload": { "command": "debug config" } }
```
- 结果：会收到 stdout 的 JSON 片段，最后 completed

### B. GET_SESSION_LIST
```json
{ "type": "GET_SESSION_LIST", "payload": {} }
```
- 结果：`ERROR` -> `SESSION_LIST_PARSE` / `unexpected end of JSON input`

### C. EXECUTE_COMMAND（session list）
```json
{ "type": "EXECUTE_COMMAND", "payload": { "command": "session list --format json --max-count 5" } }
```
- 结果：running 后无有效 JSON 输出（或输出为空），无法解析

## OSAC 需要确认/优化点（建议）
1) **GET_SESSION_LIST 解析**
   - 当 opencode 输出为空时，不应抛出解析错误，建议返回空数组 `[]` 并附警告字段。

2) **session list 输出为空/不完整问题**
   - 需确认 OSAC 是否正确调用：`opencode session list --format json`
   - 若 opencode 无输出，需记录 raw stdout/stderr 并返回给 Server 端

3) **输出丢弃日志**
   - `osac.log` 出现大量 `dropping message type=COMMAND_OUTPUT`，请检查 OSAC 是否在连接稳定时仍发生消息丢弃（可能是队列溢出或未绑定连接状态）

## 备注
- 当前端口映射在 KVM 侧有波动：`192.168.10.172:21399` 目前不可达（TCP connect fail）。
- 但 OSAC 在 VM 内确认运行，说明是链路/映射问题，非 OSAC 进程未启动。
