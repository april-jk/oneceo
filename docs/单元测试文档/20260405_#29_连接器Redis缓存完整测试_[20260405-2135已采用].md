# 2026-04-05 #29 连接器 Redis 缓存完整测试 [20260405-2135已采用]

## 1. 测试目标

验证 `GET /api/connectors/me` 的 Redis 缓存改造在以下方面满足交付要求：

1. 读取性能路径正确（cache-aside）。
2. 写操作后缓存一致性正确（失效生效）。
3. Redis 故障/关闭时可自动降级，不影响接口可用性。
4. 并发 miss 不触发重复回源风暴（同用户请求去重）。

## 2. 测试范围

代码范围：

1. `apps/api/src/routes/connector-routes.ts`
2. `apps/api/src/services/user-connector-service.ts`
3. `apps/api/src/services/connector-redis-cache-service.ts`
4. `apps/api/src/services/redis-keyspace.ts`

用例范围：

1. 路由访问与鉴权。
2. 服务层缓存命中/未命中/写回。
3. 服务层缓存失效（create/update/delete/setDefault/OAuth/auth clear/needs_auth）。
4. Redis 关闭与 Redis 故障注入降级。
5. 并发 miss 去重。

## 3. 测试环境

1. 仓库：`/Users/watson/codingProj/oneceo`
2. Node/PNPM：项目默认环境
3. API 测试执行器：`tsx --test`
4. Redis 故障注入方式：mock `connectorRedisCacheService.getMe` 抛错 + 现有 `redis-keyspace` 不可达 Redis 用例

## 4. 用例矩阵

| 编号 | 场景 | 预期 | 自动化用例 |
|---|---|---|---|
| C1 | `/api/connectors/me` 未登录访问 | 返回拒绝（401） | `connector-routes.test.ts` |
| C2 | `/api/connectors/me` 已登录访问 | 返回用户级 catalog + profiles | `connector-routes.test.ts` |
| C3 | Redis 命中 | 不回源 DB，直接返回缓存 | `user-connector-service.test.ts` |
| C4 | Redis 关闭 | 回源 DB，接口可用 | `user-connector-service.test.ts` |
| C5 | Redis miss | 回源 DB 并写回 Redis | `user-connector-service.test.ts` |
| C6 | Redis get 故障注入 | 自动降级 DB，接口可用 | `user-connector-service.test.ts` |
| C7 | 并发 miss（同 user） | 只触发一次回源 | `user-connector-service.test.ts` |
| C8 | createProfile 后一致性 | 触发缓存失效 | `user-connector-service.test.ts` |
| C9 | updateProfile 后一致性 | 触发缓存失效 | `user-connector-service.test.ts` |
| C10 | deleteProfile 后一致性 | 触发缓存失效 | `user-connector-service.test.ts` |
| C11 | setDefaultProfile 后一致性 | 触发缓存失效 | `user-connector-service.test.ts` |
| C12 | Redis 基础容错（不可达） | Redis 命令失败不阻塞主路径 | `redis-keyspace.test.ts` |

## 5. 执行命令

```bash
pnpm --filter api exec tsx --test tests/connector-routes.test.ts
pnpm --filter api exec tsx --test tests/user-connector-service.test.ts
pnpm --filter api exec tsx --test tests/redis-keyspace.test.ts
```

## 6. 执行结果（2026-04-05）

1. `tests/connector-routes.test.ts`：3 通过，0 失败。
2. `tests/user-connector-service.test.ts`：14 通过，0 失败。
3. `tests/redis-keyspace.test.ts`：4 通过，0 失败。

结论：

1. 缓存命中、miss 写回、写后失效链路通过。
2. Redis 关闭与故障注入降级行为通过，接口可用性满足要求。
3. 并发 miss 去重通过。

## 7. 交付可用性结论

在本次变更范围内，连接器加载 + Redis 缓存能力已通过完整回归，满足“可交付可用”标准。
