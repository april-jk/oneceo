## 2026-03-22

- 做了什么：
  修复 Codex 历史会话在旧 sandbox 已失效时仍尝试复用的问题；在 provision 阶段新增失效 sandbox 自动判死、清理本地 E2B 缓存并同请求冷重建；同步更新 Codex 状态归档恢复设计文档。
- 遇到什么：
  历史会话 `de852364-8bb5-4028-8348-498e4c80aa0e` 报错 `[PROVISION:commands_ready] sandbox command channel not ready: Sandbox is probably not running anymore`，说明恢复入口没有把旧 sandbox 死亡视为可自愈事件。
- 计划如何解决：
  完成类型校验后，用历史会话回归验证发送消息时是否会自动新建 sandbox 并继续走恢复链路；若仍有残余问题，再补针对 Codex send path 的用户态错误收敛。

- 做了什么（补充）：
  修复会话 `de852364-8bb5-4028-8348-498e4c80aa0e` 在内容预览中出现“暂无文件”的问题，新增后端空目录恢复与缓存回退策略，并在前端增加基于 diff 的历史文件树回退展示。
- 遇到什么（补充）：
  该会话 `workspace/dir` 实时返回为空，且最近消息缺少 `filePaths/fileChanges` 元数据，单靠运行态目录读取无法恢复历史文件感知。
- 计划如何解决（补充）：
  继续观察该会话与同类历史会话的目录回退命中情况，若仍有空态误报，再补“按会话级快照固定最近一次非空目录”的持久化策略。

- 做了什么（再次补充）：
  按用户反馈把文件树从“workspace 根目录视角”改为“项目目录优先视角”，采用仅渲染层投影方案，不改写真实文件路径。
- 遇到什么（再次补充）：
  之前的路径重写思路会让前端展示路径与后端读取路径脱节，存在文件点击后读取失败的隐患。
- 计划如何解决（再次补充）：
  继续对历史会话回归验证“项目目录优先展示 + 文件可打开”的一致性，必要时补充后端返回 `projectRootHint` 以减少前端推断。
