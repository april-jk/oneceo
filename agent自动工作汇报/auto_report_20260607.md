# auto_report_20260607

## Altus 中止后立即续发竞态定位与方案

- 做了什么：沿前端中止、stop API、coordinator abort、数据库终态和新消息 submit 链路定位竞态，并新增 `[尚未采用]` 修复方案文档。
- 遇到什么：当前候选修改只在深层 `startRun()` 增加 Redis 轮询，但正常 `/inputs` 请求会在外层提前失败；stop API 也在数据库终态完成前返回，前端随后过早解锁输入。
- 计划如何解决：待方案评审采用后，让 stop API 等待 run 终态落库，前端只依据服务端真实终态解锁，并以真实服务测试覆盖 Redis 开关两条路径。

## Altus 中止后立即续发修复完成

- 做了什么：方案确认采用后，收口活动 run 检查到 run 创建边界；stop API 等待 coordinator 写入终态；补齐执行前阶段 abort 检查；前端只在服务端返回终态后解锁输入并清理恢复态。
- 遇到什么：中止分支原先在数据库终态前执行 Sandbox memory 刷新，会继续放大停止延迟。
- 计划如何解决：已把终态写入放到首位，memory 刷新改为终态后的后台清理；API 定向测试 18 项、Web 状态测试 11 项以及 API/Web 类型检查均已通过。
