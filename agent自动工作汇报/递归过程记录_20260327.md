# 2026-03-27 递归过程记录

- 新进展：继续排查 `managed_run_tool_round_limit_exceeded` 后，确认这不是生成失败，而是 Altus 在 `complete_task` 前打满了默认 `12` 轮工具预算；本次失败 run 实际已经生成并校验了 `职业规划.pptx`。
- 处理动作：补充问题文档 `20260327_AltusManaged轮次耗尽导致已生成交付物仍失败_问题与修复方案.md`，并将 `altus-run-coordinator.ts` 的默认轮次预算调高到 `16`。
- 处理动作：同步强化 `altus-managed-prompt-service.ts` 的 completion 规则，要求下载型交付物在文件已生成并完成一次验证后立刻 `complete_task`，避免额外轮次浪费。
- 计划如何解决：重启 API 后重新跑一条真实 PPT 生成链路，验证 run 是否进入 `completed`，以及交付物卡片是否正常落到前端。
- 新进展：真实 PPT 回归已通过，测试会话 `dca169c2-e4b6-4655-ab0f-f466cd2b6487` 的 managed run `ebb167fb-7a0d-4697-959f-15e3b695e874` 最终状态为 `completed`，成功产出并下载 `career-plan-roundfix.pptx`。
- 新进展：同一日志窗口内该成功 run 共触发 `16` 次模型调用，已经超过旧默认值 `12`，说明这次轮次预算调整命中了真实根因。
- 处理动作：验证完成后已手动关闭测试 sandbox `ia1x8j56b2bjxyamxq80f`，并删除测试 session；再次查询 session 详情返回 `404`，确认清理完成。
- 新进展：用户再次复现 `managed_run_tool_round_limit_exceeded`，最新失败 run 为 `aeb9fd96-d4d3-4c80-a735-c615a100e7f1`；事件回放显示它不是卡在附件或交付物，而是反复修 `python-pptx` 导入错误，直到 16 轮预算耗尽。
- 处理动作：将默认预算继续从 `16` 提高到 `24`，并补充 office 文件生成 prompt，要求从最小工作脚本和稳定导入开始，避免先写复杂脚本再反复调错。
- 计划如何解决：重启 API 后按用户这次的相同场景重新复测，确认简单“职业规划 PPT”请求也能完成并挂出下载交付物。
- 新进展：第二轮修复后的同场景探针中，run `9f69447f-ed93-4c6d-a8be-0187be9e323d` 没有再掉进 `managed_run_tool_round_limit_exceeded`，而是进入 `waiting_user`，说明“轮次耗尽”主故障点已经被压住。
- 处理动作：探针结束后已手动关闭测试 sandbox `ij5p1bd9iqd67k0c0vzhb`，并删除测试 session；当前剩余的是“明明可以默认继续做，却还会泛化澄清”的体验问题。

- 做了什么：先确认 API 存活，再复盘最近一次失败会话，拿到“规划成功、执行环境启动失败”的精确阶段信息；随后直接用仓库内 `e2b-connector` 做创建探针。
- 遇到什么：同一配置下 E2B 创建 sandbox 连续报 `fetch failed`，而数据库、API 健康检查都不是当前阻塞点，说明故障不在消息入口。
- 计划如何解决：把问题收敛为代理配置导致的 E2B 外连失败；下一步按环境变量切换为直连，复测任务创建是否恢复，并视结果决定是否拆分本地/代理两套环境配置。
- 新进展：已确认 `127.0.0.1:7897` 有 `verge-mihomo` 监听，因此将 `apps/.env` 的代理端口改到 `7897` 后，E2B 创建立即恢复正常；任务创建 smoke 也成功拉起了测试 sandbox，随后已手动关闭清理。
- 新进展：浏览器里的 `failed to fetch` 进一步定位为前端请求目标仍是 `oneceo.ai:4000`，但本机无对应 hosts 映射；由于当前权限无法修改系统 hosts，改为将本地开发配置切回 `localhost` 并重启前后端。
- 新进展：`PPT 办公` skill 的 `request failed: 400` 已定位为附件上传时触发的 `sandbox_verify` 失败，而不是 skill 模板内容错误；关键日志为 `[PROVISION:sandbox_verify] exit status 15`。
- 处理动作：在 `sandbox-agent-provision-service.ts` 里为 Sandbox 增加 `SANDBOX_OPENAI_*` 独立环境变量覆盖，同时在 `apps/.env` 中补齐 Sandbox 专用 `API_KEY/BASE_URL/MODEL/API_TYPE`，把 Sandbox/OpenCode 直连和主平台 llm-proxy 主链路彻底隔离。
- 计划如何解决：重启 API 后走最小链路复测“draft session -> skill 附件上传”，确认不再出现 `400` 和 `sandbox_verify exit status 15`，测试完成后立即关闭测试 sandbox，避免额外计费。
- 新进展：最小链路复测已通过，`POST /api/task-creation/sessions/:sessionId/attachments` 成功上传 `skill-office-ppt.md`；测试产生的 sandbox 已主动关闭，会话也已删除，确认主故障点已解除。
- 新进展：继续复盘 `read_file .attachments/...skill-office-ppt.md does not exist` 后，已确认附件上传阶段与 Altus managed run 阶段使用了两个不同 sandbox；附件在 sandbox A，但执行发生在 sandbox B。
- 处理动作：在 `task-creation-routes.ts` 中让 `ensureTaskSessionRuntime()` 复用/创建 sandbox 时同步写入 `task_session_sandbox_bindings`，同时在 `altus-managed-setup-service.ts` 中优先复用 file-memory runtime 的 sandbox，并回写 binding，彻底消除双 sandbox 分叉。
- 新进展：针对性复测已通过，新的会话只创建了一个 sandbox，`read_file` 已成功读到 `.attachments/skill-office-ppt.md`；复测产生的 sandbox 和会话均已清理。
- 新进展：继续把“职业规划 PPT 已生成但用户找不到文件”的问题向上收敛，确认根因不是单一日志展示，而是系统没有把 sandbox 内文件升级成正式交付物。
- 处理动作：整理现有 `Altus managed run + workspace/raw + R2 archive` 链路，形成正式设计文档 `docs/agent研发文档/20260327_交付物出Sandbox闭环设计.md`，规定只有“文件已持久化并可下载”才算任务完成。
- 计划如何解决：待用户确认设计后，按最短路径依次落地 `complete_task.attachments`、交付物入库与 R2 持久化、下载接口、前端完成卡片四部分，实现办公类任务的正式交付闭环。
- 新进展：已把“文件只留在 sandbox 里”的问题收口为正式交付物闭环，并完成代码实现；现在 managed run 可以通过 `complete_task.attachments` 把最终文件持久化为 deliverable。
- 处理动作：后端新增 deliverable 表、DAO、R2 持久化服务和下载接口；前端新增 `TaskDeliverableCard`，并在 `run_completed` 的 status_update 中解析 `deliverables` 元数据。
- 新进展：最小真链路验证通过，Altus 成功生成 `deliverable-test.txt`，deliverables 列表返回 1 个文件，下载接口拿到的内容为 `deliverable pipeline ok`。
- 新进展：recent messages 回放验证也通过，`status_update -> run_completed` 已持久化 `deliverables`，说明刷新页面后下载卡片可恢复。
- 处理动作：按仓库要求，验证过程中创建的 sandbox 已手动关闭，测试 session 也已删除。
- 计划如何解决：下一步只剩用户侧真实场景复测，重点看 PPT/Word/Excel 文件在 UI 上的下载体验是否符合预期。
- 新进展：继续复盘用户“PPT 没有图片、没有排版”的反馈后，已确认根因不在单个 skill 文案，而在当前 Altus managed 工具根本没有联网检索和图片获取能力；同时上一轮为了稳定交付，又明确把办公文件约束成“优先简单正确”，因此结果自然偏素。
- 处理动作：新增设计文档 `docs/agent研发文档/20260327_PPT联网检索配图与版式增强设计.md`，明确最短路径不是只加一个 Tavily 开关，而是补齐 `web_search/web_extract -> shell_execute 下载图片 -> 固定 slide_type 版式` 的完整闭环。
- 处理动作：同步更新 `技能附件_PPT办公技能模板设计.md`，把 PPT skill 的后续演进入口指到新文档，避免实现阶段再次出现 brief 与执行链路割裂。
- 计划如何解决：待用户确认设计后，再按文档落地 Tavily connector、managed tools、prompt 约束和 PPT skill 模板更新，并在真实 PPT 场景里验证“有图、有版式、可下载”的交付效果。
