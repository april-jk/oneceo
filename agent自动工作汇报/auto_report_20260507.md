# auto_report_20260507

## 用户侧蒙层引导阶段一

- 做了什么：基于已采用的用户侧蒙层引导方案，完成首页核心引导、局部功能引导与设置弹窗引导的第一阶段实现。
- 遇到什么：设置弹窗引导最初渲染在 Radix Dialog 外部，点击“下一步”会被判定为弹窗外点击，导致设置界面关闭；移动到弹窗内部后又因为 Dialog 的 translate 定位导致 spotlight 坐标偏移；同时复现到首页引导和设置引导可能并存的问题。
- 如何解决：将引导层统一 portal 到 `document.body` 保持 viewport 坐标准确，并让设置弹窗忽略来自引导层的 outside interaction；通过引导组件的全局打开事件保持同一时间只有一个引导实例显示。
- 验证结果：`pnpm --filter web check` 通过；Playwright 复现链路确认设置引导点击“下一步”后仍留在设置弹窗并进入第 2 步；真实 Chrome 清除本地缓存后复测，首页四步与设置两步高亮位置、下一步行为均符合预期。

## 用户侧蒙层引导阶段二

- 做了什么：继续补充真实 `/projects` 页面项目结构引导、创建项目弹窗字段引导，以及 `/project/:id` 项目详情引导。
- 遇到什么：最初误把引导加到了当前路由未使用的旧 `Projects.tsx`；实机验证后确认真实项目页为 `ManagerView`。项目总览中部分锚点选区过大，会导致滚动容器跳到项目卡片中部。
- 如何解决：撤回旧 `Projects.tsx` 的临时改动，将引导锚点落到 `ManagerView`、`ProjectEditorDialog` 和 `ProjectDetail`；将项目总览锚点收窄为创建按钮、普通项目区、自组织项目折叠栏、项目卡片头部和经理行。
- 验证结果：`pnpm --filter web check` 通过；真实 Chrome 复测项目总览、创建项目弹窗、项目详情页引导均可触发，弹窗点击“下一步”不会退出。

## 用户侧蒙层引导阶段三

- 做了什么：补充 `/ceo-view` 总经理视图导览，并为设置页的 Connectors、积分与消费 tab 增加局部导览。
- 遇到什么：设置页局部导览需要和设置总览导览互斥，同时已完成的 tab 导览不能残留旧的 focused tour 状态。
- 如何解决：设置总览在 Connectors / 积分 tab 中不主动弹出；局部导览按当前 tab 单独判断完成状态，已完成时清空 focused tour；继续沿用 `GuidedTour` 的全局互斥事件和 Dialog outside interaction 拦截。
- 验证结果：真实 Chrome 确认设置弹窗 tab 切换不会退出；Playwright 登录测试账号复测 Connectors 与积分导览均可触发，点击“下一步”后设置弹窗仍保留。

## 用户侧蒙层引导补充入口

- 做了什么：在 `设置 > 设置` 增加“引导”分区，提供“重放设置引导”和“重新加载全部”两个入口。
- 遇到什么：方案中要求允许回看，但第一轮实现只记录了完成状态，没有提供用户可见的重置入口。
- 如何解决：统一清除当前已实现的用户侧导览 localStorage key；点击后立即重放设置分区引导，其他页面在下次进入时重新触发。
- 验证结果：`pnpm --filter web check` 通过；真实 Chrome 确认入口可见，点击“重新加载全部”后设置导览立即重放且弹窗不关闭。

## 用户侧蒙层引导阶段四补充

- 做了什么：按 CEO 反馈把单一“体验示例任务”升级为 Manus 风格的能力入口条，首屏展示制作幻灯片、创建网站、开发应用、深度研究、分析表格和“更多”，更多菜单覆盖项目拆解、修复问题、资料整合、数据可视化、文档/PDF、经营复盘、部署上线。
- 遇到什么：oneceo 能力边界不能简单照搬 Manus 的视频、音频等分类；引导也不能退回到按钮说明。
- 如何解决：基于 oneceo 当前产品结构和任务塔台定位，给每个能力入口配置独立示例 prompt 和推荐模型；点击任一入口后沿用现有蒙层演示链路，演示写需求、归属项目、启用上下文、选择模型并准备发送，仍不自动发送。
- 验证结果：`pnpm --filter web check` 通过；真实 Chrome 确认能力入口可见、更多菜单可展开、点击 `创建网站` 会启动演示、第二步自动填充对应 prompt、模型步骤会切到 Agent Max 并展开模型菜单。

## 用户侧蒙层引导阶段四精简

- 做了什么：按最新反馈移除首页能力演示下方的“示例任务会产出”预览卡片。
- 遇到什么：该卡片在输入区下方占用较大视觉空间，让主页变重。
- 如何解决：删除预览卡片渲染、对应的交付物引导步骤，以及仅服务该卡片的交付物配置。
- 验证结果：`pnpm --filter web check` 通过；真实 Chrome 确认点击能力入口后只高亮能力入口区，页面下方不再出现“示例任务会产出”卡片。

## 用户侧能力入口口径调整

- 做了什么：将更多菜单中偏功能机制的 `连接器协作`、`总经理视图`、`Skills / MCP` 替换为面向任务结果的 `资料整合`、`经营复盘`、`部署上线`。
- 遇到什么：原入口更像平台内部功能表现，不像“oneceo 能帮用户完成什么事”的能力表达。
- 如何解决：保留背后的连接器、CEO 视角和专业能力，但在用户可见入口中改为结果导向的任务名称，并同步更新示例 prompt。
- 验证结果：`pnpm --filter web check` 通过；真实 Chrome 确认更多菜单显示 `资料整合`、`经营复盘`、`部署上线`，不再显示 `连接器协作`、`总经理视图`、`Skills / MCP`。

## 首页引导触发修正

- 做了什么：修正首页引导触发逻辑，避免完成整套引导后，用户再点连接器、模型、附件时又零碎弹出单独导览。
- 遇到什么：原实现把首页核心导览和局部点击导览拆成两套，导致“已经做过一次统一引导”之后，还会因为后续点击再次打断用户。
- 如何解决：将连接器和模型并入首页核心引导，删除首页局部 `local tour` 点击监听与单独弹出逻辑，改为首次统一引导一次，后续不再补弹。
- 验证结果：`pnpm --filter web check` 通过；项目重启后用 Playwright 复测，完成首次引导后再次点击连接器 / 模型不会再弹二次局部导览。

## 设置引导触发修正

- 做了什么：修正设置弹窗引导逻辑，避免总览引导结束后，切到 `Connectors` 或 `积分与消费` 再单独弹出第二套局部引导。
- 遇到什么：设置弹窗原本同时存在 overview tour 和 focused tab tour，两套机制叠加会造成“已经看完一轮，后面还会再补弹”的体验。
- 如何解决：将 `Connectors` 与 `积分与消费` 的说明并入同一套设置总览引导，在引导步骤中自动切换 tab；删除后续 tab 切换时的 focused tour 触发逻辑。
- 验证结果：`pnpm --filter web check` 通过；项目重启后用 Playwright 复测，完整走完设置引导后再次点击 `Connectors`、`积分与消费` 均不会再出现新的引导浮层。

## 设置引导步数修正

- 做了什么：修正设置弹窗统一引导的跨 tab 续播逻辑，避免面板显示 `1 / 8`，实际点到第 2 步就结束。
- 遇到什么：`GuidedTour` 原来只会跳到“当前 DOM 里已经能找到目标节点”的下一步；设置引导第 3 步开始位于 `Connectors` tab，第 2 步点击下一步时目标尚未挂载，组件会误判为没有后续步骤并提前完成。
- 如何解决：为 `GuidedTour` 增加允许进入“待解析下一步”的能力；设置引导在切换到 `Connectors` / `积分与消费` 时主动触发一次重新定位，让高亮在 tab 内容挂载后继续对准正确区域。
- 验证结果：`pnpm --filter web check` 通过；Playwright 复测可连续看到 `1 / 8`、`2 / 8`、`3 / 8`、`4 / 8`、`5 / 8`，并成功切入 `Connectors` tab，不再在第 2 步提前结束。

## 滚动容器锚点修正

- 做了什么：修正设置引导在 `Connectors` 列表和 `消费记录` 列表中的高亮范围，避免在固定高度、内部滚动的面板上整块发亮。
- 遇到什么：原来的锚点分别绑在连接器目录容器和消费记录容器上，导致 spotlight 会覆盖整段可滚动区域，视觉上像蒙层显示异常。
- 如何解决：把 `授权与管理` 步骤改为高亮首个可见连接器卡片，把 `消费记录` 步骤改为高亮记录区标题栏，不再直接锚定整个滚动容器。
- 验证结果：`pnpm --filter web check` 通过；Playwright 复测显示 `授权与管理` 的 spotlight 收敛到约 `312.5 x 106`，`消费记录` 收敛到约 `669 x 48`，不再出现整块列表被框起的情况。

## 引导锚点全量复查

- 做了什么：复查当前用户侧全部 `data-tour` 锚点，重点排查列表页、项目页和总经理视图中容易被整块高亮的大容器。
- 遇到什么：项目总览中的普通项目分区、自组织项目统计行、经理行，以及 CEO 欢迎说明都存在“语义上要讲局部，但锚点落得偏大”的情况。
- 如何解决：将普通项目说明收口到分区标题行；将自组织项目卡片说明收口到统计摘要行；将经理与员工说明收口到经理头部行；将 CEO 上下文说明收口到欢迎气泡本身。
- 验证结果：`pnpm --filter web check` 通过；Playwright 实测确认新的锚点都能正常命中，且不再出现设置页那种整块滚动区被高亮的异常表现。

## 能力入口演示触发与示例提示词优化

- 做了什么：修正首页能力入口“每点一次都重复弹同一套蒙层”的行为，并补充 `制作幻灯片`、`创建网站` 的 Manus 风格示例提示词卡片。
- 遇到什么：原实现里每次点击能力入口都会强制重新打开场景演示，引导内容相同，重复感很强；同时入口只给一个默认 prompt，不够像实际任务起点。
- 如何解决：保留“点击能力入口会融入示例 prompt”的动作，但把蒙层演示限制为首次完成前只出现一次；演示完成或跳过后，后续点击只更新 prompt 和推荐模型，不再补弹。同一时间为 `制作幻灯片` 增加 4 个示例提示词卡片，为 `创建网站` 增加 `着陆页 / 仪表盘 / 作品集` 分类与示例卡片。
- 验证结果：`pnpm --filter web check` 通过；Playwright 复测确认第一次点击能力入口会弹引导，点击 `跳过` 后再点其他入口不再弹引导，但输入框会继续写入对应 prompt；网站入口下方的分类和示例卡片可见且点击后会更新输入框内容。

## Manus 对齐修正（输入框模式化）

- 做了什么：根据真实 Manus 页面对比，补齐 oneceo “能力模式进入输入框本体”的交互，并压轻示例区视觉权重。
- 遇到什么：原结构虽然有能力入口和示例卡片，但输入框底部动作区缺少当前能力标签，体验仍像“额外组件”而不是“当前工作模式”；网站能力区也缺少参考入口动作。
- 如何解决：在输入框动作栏新增当前能力标签（如 `创建网站`、`文档/PDF`），点击能力后同步切入该标签；示例区改为更轻的分类与短卡结构；在网站分类区新增 `添加网站参考` / `从 Figma 导入` 快捷动作，直接把参考模板注入输入框。
- 验证结果：`pnpm --filter web check` 通过；使用 Computer Use 对比 `oneceo.ai:3000/home` 与同窗口 `manus.im/app`，确认能力标签、分类区和探索卡片层级已对齐，且不再重复弹同一套蒙层引导。

## Manus 对齐修正（全能力示例 + 自动技能）

- 做了什么：把能力入口从“PPT/网站较完整、其他较简化”的状态升级为全能力统一体验，并接入能力到 `skill/mcp` 的自动关联。
- 遇到什么：用户反馈除 `制作幻灯片`、`创建网站` 外，其它入口只有单条简单示例，不够像可直接执行的任务工作台；同时能力入口缺少自动挂载技能链路。
- 如何解决：为 `开发应用`、`深度研究`、`分析表格`、`项目拆解`、`修复问题`、`资料整合`、`数据可视化`、`文档/PDF`、`经营复盘`、`部署上线` 补齐多条探索卡片；新增能力自动匹配逻辑，根据能力关键词从技能目录和已授权 custom MCP 中匹配并挂载引用标签（前缀 `auto-capability:`），切换能力时自动替换，用户可手动移除。
- 验证结果：`pnpm --filter web check` 通过；Computer Use 实测点击 `制作幻灯片` 后，输入框内自动出现 `skill PPT 工作流` 标签，同时保留示例提示词和模型切换行为。

## 首页引导重构（单次完整演示）

- 做了什么：按 CEO 新要求重构用户侧首页引导，移除 `项目`、`总经理视图` 相关蒙层，并把新建任务页改成首次打开时一次性完整演示。
- 遇到什么：原实现拆成 `home core` 与 `scenario demo` 两套 tour，导致用户第一次进入首页和后续点击能力入口时会分开重复弹出，引导节奏被打散。
- 如何解决：统一为 `oneceo:tour.home_new_task.completed` 一套状态键；首次进入时自动切到 `制作幻灯片` 能力上下文，但不写入 prompt，而是先演示“选能力 -> 选示例 -> 编辑输入框 -> 选项目/连接器/模型 -> 发送”的完整链路；同步移除项目概览、项目创建、项目详情、总经理视图中的 tour 组件；为 `GuidedTour` 增加更明显的遮罩淡入、spotlight 弹性过渡和卡片切步动画。
- 计划如何解决：接下来继续用 Computer Use 在真实 Chrome 页面回归，重点看首次进入只播一次、能力点击不再补弹、动画切换是否顺滑。

## 设置引导移除

- 做了什么：按新要求移除设置弹窗里的 `设置分区` 蒙层引导，并收口“引导”分区的重播入口。
- 遇到什么：设置页虽然已经不再适合作为首次教学路径，但界面里仍然保留了 `重放设置引导`、自动弹出 tour 和相关提示文案，体验上会和“首页只演示一次”的新规则冲突。
- 如何解决：删除设置页的 `GuidedTour` 挂载、状态控制和分步切 tab 逻辑；将设置中的引导文案改成只重置首页 `新建任务页` 的一次性引导，并把按钮收敛为单个 `重新加载引导` 入口。
- 验证结果：准备执行 `pnpm --filter web check`，并在设置弹窗中复看是否不再弹出任何蒙层。

## 当前能力胶囊可关闭

- 做了什么：给输入框动作栏里的当前能力标签增加 hover 关闭动作，交互对齐 Manus。
- 遇到什么：原来的当前能力标签仍然是“再次点一下会重复选中能力”的按钮，缺少清晰的退出路径，用户无法一眼看出怎么取消当前能力上下文。
- 如何解决：把当前能力改成能力胶囊，默认展示能力图标和名称；鼠标移入后，在图标位切换成关闭按钮。点击关闭后，只清除当前能力选择以及它自动挂载的 skill / mcp，不清空用户已经输入的任务内容。
- 验证结果：准备执行 `pnpm --filter web check`，随后在浏览器里确认 hover 时出现关闭入口，点击后能力胶囊与示例区一起消失。

## 引导完成状态按用户隔离

- 做了什么：修正首页引导完成状态的存储方式，避免同一浏览器下换账号登录后不再触发引导。
- 遇到什么：原实现直接把 `oneceo:tour.home_new_task.completed` 写在浏览器全局 `localStorage`，没有带用户身份；只要一个账号看过，引导就会对后续账号一起失效。
- 如何解决：给 `GuidedTour` 增加用户作用域 key 解析逻辑，首页引导状态改为按 `user.id` 存储；设置里的“重新加载引导”同时清理旧的全局 key 和当前用户的新 key。
- 验证结果：准备执行 `pnpm --filter web check`，并用不同账号在同一浏览器中验证新账号首次登录时会重新触发引导。
