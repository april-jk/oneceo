# KVM 联调记录（交付）

- 日期: 20260210-000216
- 目的: 复测 OSAC 端口映射与连通性（sandbox -> host 端口转发）

## 关键环境信息
- KVM Orchestrator: http://192.168.10.172:8500
- OSAC 监听端口: 18080
- 连接方式: port-mapping

## 本次复测会话信息
- sessionId: sess_0042e555cc414cae
- VM: sandbox_sess_0042e555cc414cae
- VM IP: 172.28.0.172
- 端口映射:
  - host_port 21323 -> vm_port 18080
  - host_port 20020 -> vm_port 18080

## 实际操作步骤
1. 创建 sandbox 后等待 40 秒（给 DHCP/IP 与 OSAC 启动留时间）。
2. 调用刷新端口映射:
   - GET /v1/sandboxes/sess_0042e555cc414cae/ports
3. 计划使用端口测试:
   - Test-NetConnection 192.168.10.172 21323

## 结果与现象
- 第 2 步获取的 ports 返回端口映射存在且指向 vm_ip=172.28.0.172。
- 第 3 步在执行前被用户中断，未拿到最终连通性结果。

## 备注
- 之前多次出现映射端口连接被拒绝 (ECONNREFUSED) 的情况，怀疑 VM 内 OSAC 未就绪或端口映射未随 IP 刷新。
- 本次严格按“等待 -> refresh -> 测试”的流程执行，但测试被中断，建议再次跑完并确认结果。

## 建议 KVM 团队核查
1. ports 刷新后，映射规则是否确实指向当前 VM IP。
2. VM 内 OSAC 是否已监听 18080（可用 guest-exec 验证 ss/netstat）。
3. 若映射存在但连接拒绝，检查 iptables 规则是否正确下发到 PREROUTING/OUTPUT。
