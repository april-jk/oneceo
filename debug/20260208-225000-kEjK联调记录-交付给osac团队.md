# OSAC 联调记录（复测，交付给 OSAC 团队）

## 基本信息
- 日期时间: 2026-02-08
- session_id: sess_c0162934ca15470f
- VM IP（原始）: 172.28.0.172:18080
- 端口映射（转发）: 192.168.10.172:21612
- OSAC token（本次测试用）: 0a40641838fd4a6fbbe723f25573f804

## 环境准备
- OSAC/opencode 已下发到 /opt/.altus/opencode
- chmod +x 已执行
- OSAC 以 setsid 后台方式启动，确认监听 18080

## 复测结果（OSAC WebSocket）
WebSocket：`ws://192.168.10.172:21612/ws`
Auth：`Authorization: Bearer <OSAC_AUTH_TOKEN>`

### 1) EXECUTE_COMMAND: debug config
请求：
```json
{ "type": "EXECUTE_COMMAND", "payload": { "command": "debug config" } }
```
结果：
- 多条 `COMMAND_OUTPUT`（stdout）返回 JSON 片段
- `COMMAND_STATUS: completed`
- ✅ 正常返回

### 2) EXECUTE_COMMAND: session list --format json --max-count 5
请求：
```json
{ "type": "EXECUTE_COMMAND", "payload": { "command": "session list --format json --max-count 5" } }
```
结果：
- stdout 返回：`scanner error: read |0: file already closed`
- `COMMAND_STATUS: completed`
- ❌ 未返回有效 JSON

### 3) EXECUTE_COMMAND: run --format json "请输出 Hello from opencode"
请求：
```json
{ "type": "EXECUTE_COMMAND", "payload": { "command": "run --format json \"请输出 Hello from opencode\"" } }
```
结果：
- 收到 `COMMAND_STATUS: running`
- 随后连接被取消（未收到 completed/failed）
- ❌ 无完整结果

## 关键异常点
1) `session list --format json` 触发 `scanner error: read |0: file already closed`
   - 表现为 stdout 输出错误文本而非 JSON
   - 说明 OSAC 对 opencode 输出的读取/扫描存在提前关闭管道的问题

2) `run --format json` 长时间 running 无完成
   - 可能 OSAC 未正确处理 opencode 的 stdout/stderr 流或进程退出信号

## 建议 OSAC 侧排查方向
- **stdout/stderr 读取逻辑**：确认 scanner/reader 不会在子进程仍输出时关闭 pipe。
- **opencode 子进程生命周期**：确保完成后回传 `COMMAND_STATUS` completed/failed。
- **空输出/非 JSON 处理**：若 opencode 无输出，应返回 `[]` 或可解析的空 JSON。
- **长任务超时策略**：`run --format json` 是否因无交互输入而阻塞；建议显式设置超时和返回错误。

## 备注
- 当前端口映射可达（TCP 连续探测 5 次成功）。
- OSAC 监听稳定，问题集中在 opencode 输出处理与命令执行回传。
