# KVM-Orchestrator 联调记录（编排侧复测失败）

## 结论
- OSAC 本体正常（前台运行可监听 18080）
- 本次按你们建议改为 `path+args` 且 `capture_output=false` 后，**仍无日志且无监听**
- 说明 **guest-exec 执行链路仍未真正执行启动命令** 或命令被截断/丢失

## 复测关键信息
- session: `sess_cbab9b91773c499f`
- sandbox: `sandbox_sess_cbab9b91773c499f`
- provision 返回：`ws://192.168.10.172:21648/ws`
- OSAC 连接：`OSAC WebSocket 连接超时`

## 启动命令（编排端实际发送）
使用 `path + args`，`capture_output=false`：
```json
{
  "path": "/bin/bash",
  "args": ["-lc", "mkdir -p /opt/.altus/opencode/log /opt/.altus/opencode/tmp && mv -f /opt/.altus/opencode/osac-linux-amd64_v1.0 /opt/.altus/opencode/osac && mv -f /opt/.altus/opencode/opencode /opt/.altus/opencode/opencode && chmod +x /opt/.altus/opencode/osac /opt/.altus/opencode/opencode && setsid env OSAC_AUTH_TOKEN='<token>' OSAC_LISTEN_ADDR=':18080' OSAC_OPENCODE_PATH='/opt/.altus/opencode/opencode' OSAC_LOG_DIR='/opt/.altus/opencode/log' OSAC_UPDATE_TMP='/opt/.altus/opencode/tmp' OPENCODE_BIN='/opt/.altus/opencode/opencode' OPENCODE_PATH='/opt/.altus/opencode/opencode' /opt/.altus/opencode/osac >> /opt/.altus/opencode/log/osac.log 2>&1 < /dev/null &"],
  "capture_output": false,
  "timeout_seconds": 30
}
```

## 现场验证
- `ss -lntp | grep 18080` -> `no_listen`
- `/opt/.altus/opencode/log/osac.log` -> `no_log`

## 需要 KVM 团队核对
1) guest-exec 是否会截断或拒绝长 args（尤其含大量空格/重定向/引号）
2) 是否存在字符串长度上限或 JSON 解析问题导致 args 不完整
3) 是否会忽略 `capture_output=false` 的场景
4) 是否建议改用分步 exec（mkdir/chmod/setsid 分离）

## 可提供的追踪点
- exec 通过 session: `sess_cbab9b91773c499f`
- 如需我补 jobId / 完整请求体与响应，请告知，我可立即补充
