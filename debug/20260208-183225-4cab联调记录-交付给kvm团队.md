# KVM-Orchestrator 联调记录（交付）

## 基本信息
- 日期时间：2026-02-08 18:32:25
- 目标：OSAC/opencode 通过 kvm-orchestrator guest-agent 下发并启动，使用端口映射模式连接
- 编排端环境：Windows，本地 API `http://localhost:4000`
- KVM-Orchestrator：`http://192.168.10.172:8500`

## 关键配置
- OSAC 连接模式：`port-mapping`
- OSAC 端口：18080
- 端口映射范围：20000-21999
- 下发方式：`deliveryMode=guest-agent`
- 目标路径：`/opt/.altus/opencode/osac`
- 目标 VM：按 session 创建 sandbox `sandbox_<session_id>`

## 复测流程（简要）
1. 通过 `POST /v1/sessions` 创建 session
2. 通过 `POST /v1/sandboxes` 创建 sandbox（自动 bind）
3. 通过 `POST /v1/sessions/{session_id}/files` 下发文件（guest-agent）
4. 轮询 `GET /v1/jobs/{job_id}` 观察结果

## 失败复现与错误详情
- 复测 session：`sess_df45dcbc77bc4c39`
- sandbox：`sandbox_sess_df45dcbc77bc4c39`
- 上传请求：
  - `POST /v1/sessions/sess_df45dcbc77bc4c39/files`
  - file：`osac-linux-amd64_v1.0`（约 10MB）
  - targetPath：`/opt/.altus/opencode/osac`
  - overwrite：`replace`
  - deliveryMode：`guest-agent`
- 返回 job：`job_93d134da0439480e`

**最终结果**（`GET /v1/jobs/job_93d134da0439480e`）：
- status: `failed`
- error.code: `INTERNAL_ERROR`
- error.type: `internal_error`
- error.details.reason: **`[Errno 7] Argument list too long: 'virsh'`**
- error.details.exception: `OSError`

该错误导致 OSAC/opencode 未写入 VM，OSAC 未启动，端口映射可见但连接被拒绝。

## 端口映射验证
- `GET /v1/sandboxes/sess_0349bd4f5e3144d9/ports`
  - host_port: `21340` -> vm_port: `18080` (tcp)
  - vm_ip: `172.28.0.172`
- 连接测试：`ws://192.168.10.172:21340/ws`  -> **ECONNREFUSED**
- VM 内部检查：18080 未监听（OSAC 未启动）

## 进一步排查建议（给 kvm-orch）
- `guest-agent` 文件下发内部是否通过 `virsh qemu-agent-command` 携带大参数（base64/内容）导致命令行过长。
- 建议改为：临时文件 + qga 引用路径，或使用 chunk/stream 方式，避免命令行过长。
- 大文件（opencode 150MB）更容易触发该问题。

## 附：关键请求/响应（摘录）
- jobId：`job_93d134da0439480e`
- error.details.reason：`[Errno 7] Argument list too long: 'virsh'`

---

如需更多日志/复现脚本，可继续提供。
