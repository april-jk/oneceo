# 递归过程记录 2026-03-06

1. 先确认现有链路：已存在 R2 归档、恢复和空闲归档任务，但缺少关闭前最终归档与活跃周期同步。
2. 增强归档服务：增加归档哈希、快照对象、元数据清单和恢复候选回退，恢复前先清空工作区。
3. 增强生命周期：在 `closeEnvironment` 里先归档再销毁，确保销毁前文件落盘。
4. 增强调度策略：归档任务加入 `periodic_sync`，并在 idle pause 前强制完成最新归档。
5. 更新配置示例：新增归档开关与同步间隔配置项。
6. 增强活动识别：补全 OpenCode SSE、OSAC 消息接收、SDK 命令发送等路径的 `lastActiveAt` 更新。
7. 引入脏标记：新增 `pendingArchiveUpdate/archiveDirty`，文件变更时只打标，不立即上传。
8. 调整归档触发：由固定周期归档改为“按 E2B inactivity timeout 窗口触发归档 + pause”。
9. 完善一致性：归档后自动清理脏标记，且在 hash 未变化时跳过对象上传并保持正确 snapshot key。
10. 制定测试目标：新增 `apps/api/tests/archive-persistence-test-targets.md`，定义 `T1~T5` 覆盖范围与验收点。
11. 增强可测性：为 `sandbox-archive-service` 与 `sandbox-archive-job` 增加 test deps 注入与 reset 能力，避免真实 E2B/R2 依赖。
12. 编写单元测试：新增 `sandbox-activity.service.test.ts`、`sandbox-archive.service.test.ts`、`sandbox-archive-job.service.test.ts`，覆盖自动保存/更新/恢复全链路。
13. 编写执行脚本：新增 `apps/api/scripts/run-archive-persistence-unit-tests.sh`，执行目标文档检查 + 测试集回归 + 覆盖文件校验。
14. 修复测试阻断：为测试命令增加默认 `DATABASE_URL`，并在归档测试初始化中注入 R2 必需环境变量。
15. 执行验证：`pnpm --filter api test` 与 `pnpm --filter api test:archive-flow:run` 均通过（9 项测试全部通过）。
16. 日志定位 session `e3499256-b83c-4830-a9af-d0346c0eeb64`：确认出现用户回声、`partId=text` 串流混合导致文本错序、同轮多条 final 重复展示。
17. 后端修复：`opencode-event-stream-service` 不再落盘 text delta；`opencode-remote-service` 对无 `partId` 分片直接跳过，直连模式 `flushTextStreams` 改为 `latest` 持久化，并过滤与最近用户输入一致的回声。
18. 前端修复：`opencode_user_input` 映射为用户消息；`buildChatItems` 增加同轮 final 去重、用户回声过滤、已 final 文本分片抑制，修复历史脏数据展示。
19. 验证：`pnpm --filter api test:archive-flow` 通过，`apps/web` 下 `pnpm exec tsc --noEmit` 通过。
