# 2026-02-23 Diff 预览细化

## 目标
- Diff 仅新增/仅删除时改为单列展示（不展示对比）。
- 空 diff 不进入预览列表，避免点击后无内容。

## 调整范围
- Web 前端 Diff 预览逻辑与渲染。

## 变更摘要
- `session.diff` 结构化 diff 为空或无实质变化时直接忽略。
- Diff 渲染按文件级别判断展示模式：
  - 仅新增：只展示新增内容。
  - 仅删除：只展示原始内容。
  - 其他情况：保留左右对比。

## 影响
- 提升预览可读性，减少空 diff 干扰。
