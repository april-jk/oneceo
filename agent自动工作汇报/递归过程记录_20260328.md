# 2026-03-28 递归过程记录

- 新进展：重新接上前一轮未收尾的 Tavily + PPT 配图实现，先复查现有 connector、managed tool、prompt、skill 模板和临时 smoke 脚本，确认主功能已接入，残余问题集中在 managed run 稳定性。
- 处理动作：把 `altus-run-coordinator.ts` 的默认轮次预算从 `24` 提高到 `32`，并在 `apps/.env` 增加 `ALTUS_MANAGED_MAX_TOOL_ROUNDS=32`，确保视觉型 PPT 的真实路径有足够预算。
- 处理动作：继续收紧 `altus-managed-prompt-service.ts`，新增“bounded retrieval path”“不要重复执行同一个失败命令”“职业规划等广义办公主题默认直接推进”“python-pptx 使用稳定导入”的约束。
- 处理动作：更新 `apps/web/client/src/lib/skill-attachment-templates.ts` 中的 `PPT 办公` 模板，补充联网短链路、稳定导入和“生成后立即交付”的执行要求。
- 新进展：重新启动本地 API / Web，确认 `http://127.0.0.1:4000/health` 返回 `ok`、`http://127.0.0.1:3000` 返回 `200`。
- 处理动作：执行真实 smoke `pnpm --filter api exec tsx scripts/_tmp/_tmp_altus_managed_ppt_visual_smoke.ts`，验证链路是否真正命中 `web_search / web_extract / 配图 / deliverable 下载`。
- 新进展：smoke 成功，run `2501e2d1-62ad-4e33-adea-879d608f1a4c` 进入 `completed`，最终生成 `career-plan-visual.pptx`，并验证 `6` 页中有 `4` 页包含图片。
- 处理动作：确认本次 smoke 产生的 sandbox `inmuud1a9cc81r2oqgwth` 已关闭，测试 session `6a9de98b-8195-40ef-83da-77ebf1e18358` 已删除，避免额外计费。
- 新进展：`pnpm --filter web check` 通过；`pnpm --filter api exec tsc --noEmit --pretty false` 仍被仓库内既有错误拦住，但不影响本次真实链路已经跑通的结论。
- 计划如何解决：当前先维持这套配置作为可用版本，后续重点观察不同主题的视觉质量是否稳定；如再出现特定场景退化，再继续精修 prompt 和 PPT skill 模板，而不回退主方案。
- 新进展：用户继续追问“其他 skills 是否联动 web 搜索”，因此把范围从 `PPT` 扩到 `Word` 和 `Excel`，不再只停留在平台层已有联网工具。
- 处理动作：新增设计文档 `docs/agent研发文档/20260328_Office技能联网联动扩展_设计与实现.md`，明确 `Word/Excel` 的最短实现方式是补 prompt 与 skill 模板，而不是再做一套新 connector。
- 处理动作：在 `altus-managed-prompt-service.ts` 中增加 `docx/xlsx` 的联网检索、来源保留和“不要泛化追问创建还是修改”的规则，并补充 `complete_task.attachments` 必须是真实 JSON 数组。
- 处理动作：在 `apps/web/client/src/lib/skill-attachment-templates.ts` 中更新 `Word 文档` 与 `Excel 表格` 模板，要求素材不足时优先走“搜索 -> 抽取”短链路，并在文件验证后立刻完成交付。
- 处理动作：新增通用 smoke 脚本 `apps/api/scripts/_tmp/_tmp_altus_managed_office_smoke.ts`，用于验证指定 Office skill 是否真的触发 `web_search / web_extract` 并产出 deliverable。
- 新进展：第一次 `Word` smoke 虽然成功生成 `.docx` 且确实用上联网工具，但 `complete_task.attachments` 被模型错误输出成字符串，导致 deliverable 没落库。
- 处理动作：继续收紧 prompt，明确 `attachments` 必须是真实 JSON 数组；之后重跑 `Word` smoke，最终生成并下载 `ai-pm-career-plan.docx`，问题收口。
- 新进展：`Excel` smoke 也已通过，最终生成 `ai-pm-learning-plan.xlsx`，并验证工作簿包含 `技能路线对比 / 学习资源汇总 / 学习阶段规划 / 数据来源说明` 4 个 sheet。
- 处理动作：本轮用于 `Word/Excel` 回归的 sandbox `ixrk6owk0ltavbldk9eng`、`ixtca05xmsoef2sli9cwo`、`iaqyqkiqzvlbg08xforn3` 均已关闭；测试 session 也全部删除。
- 计划如何解决：当前 Office 三件套已全部显式联动联网检索，下一步主要观察真实用户任务里的内容质量一致性，而不是再补主流程能力。

- �½�չ���û����� `Word �ĵ�` skill ������ҵ�ƻ���ʱ���� `Altus managed ����ʧ�ܣ�{"success":false,"error":{"code":"INTERNAL_ERROR","message":"�����쳣�����Ժ�����"}}`
- �����������ز�ʧ�� run `d69ea6e5-b1d1-408d-9205-6ed7039e7ee4` ���¼�����ȷ��ʧ��ǰ�Ѿ����� `read_file / web_search / write_file / shell_execute / write_file`
- ����������ͳ�Ƹ� run �Ĺ��߲���������ȷ������ `write_file` �� arguments ���ȷֱ�ﵽ `13774` �� `37427`
- �������������ع���Լ `130178 bytes` �Ĵ�������ֱ�Ӵ� `/api/llm-proxy/v1/chat/completions`���ɹ�����ͬ�� `500 INTERNAL_ERROR`
- ����������`/api/llm-proxy` ʹ��Ĭ�� `express.raw()` ��δ��ʽ�Ŵ� body limit�����´��������ڽ��� llm-proxy �߼�ǰ�ͱ� Express ����
- ������������ `apps/api/src/index.ts` ��Ϊ `/api/llm-proxy` ���� `LLM_PROXY_BODY_LIMIT_MB` ���ã�Ĭ�ϷŴ� `64mb`������ǯ�Ƶ� `128mb`
- �ƻ���ν�������� API ������ô�������̽����֤��ȷ�ϲ��ٷ���ͨ�� `500 INTERNAL_ERROR`
- �½�չ��API �������������µ� `LLM_PROXY_BODY_LIMIT_MB=64` ���ã�`/health` ��������
- ��������������Լ `130178 bytes` �� llm-proxy ̽�룬�����ѷ��� `200` ��ʽ��Ӧ�����ٸ���ͨ�� `500 INTERNAL_ERROR`
- �½�չ������ `Word �ĵ�` ��ҵ�ƻ�������·��run `763b2cd6-a82e-4929-87b4-0c5170db38f6` �� `completed`
- ����������ȷ�� deliverable `smart-building-business-plan.docx` �������أ���С `43554 bytes`
- �������������λع������ sandbox `iwcechj9lb3cupbet4a1p` �ѹرգ��Ự `b08e8430-19c4-4918-9763-ea62bef66084` ��ɾ��
- 新进展：用户反馈“选择 skill 后，发送消息要等接近 1 分钟才出现 managed run 已创建”，因此继续回到发送前链路复盘前端与 runtime 启动顺序。
- 处理动作：结合既有实测数据重新确认根因，新建 session 约 `0.36s`、首次上传 77 bytes 的 skill markdown 约 `34.2s`、单独 `runtime/start` 约 `31.1s`，而 run 到 `run_ack` 仅 `0.3s ~ 0.7s`，说明慢点在 skill 附件上传触发的 runtime 冷启动，而不是 managed run 创建。
- 处理动作：在 `apps/web/client/src/lib/task-attachments.ts` 中新增 skill 文件元数据打标、`partitionPendingAttachments()` 和 inline skill prompt 拼接逻辑，把 skill 从“上传型附件”改为“内联型说明”。
- 处理动作：在 `apps/web/client/src/components/AttachmentPickerButton.tsx` 中给内置 skill 文件增加 `inline_skill` 标记；在 `apps/web/client/src/pages/Home.tsx` 与 `apps/web/client/src/components/TaskCreationChat.tsx` 中改为只上传真正文件附件，skill-only 场景直接发送消息，不再触发 `/attachments`。
- 新进展：`pnpm --filter web check` 已通过。
- 处理动作：执行最小化结构验证，结果显示 skill-only 时 `uploadableCount = 0`、`inlineCount = 1`，skill + 普通文件时只有普通文件进入上传队列，证明 skill 已经不会再阻塞发送前链路。
- 处理动作：补充文档 `docs/agent研发文档/20260328_技能附件阻塞ManagedRun创建_问题与修复方案.md`，并更新 `docs/agent研发文档/技能附件_PPT办公技能模板设计.md` 说明新的 skill 传递方式。
- 计划如何解决：下一步观察用户真实会话里的体感延迟是否回落到秒级；如果仍有残余慢点，再继续沿 run 创建后的链路做定点排查，而不回退这次结构修复。
- 新进展：用户反馈“发送消息后，有时候看不到自己发出的消息”，于是继续沿 `sendChatInput -> loadHistory -> 消息列表渲染` 的链路排查。
- 处理动作：确认前端在 `sendChatInput(...)` 中会先乐观插入一条本地 `user_input`，但 `sessionId` 一旦变化，会立刻触发 `loadHistory(sessionId)`；而 `loadHistory(...)` 内部原先存在 `setMessages(cached.messages)` 和 `setMessages(normalized)` 两处整包覆盖。
- 根因收敛：如果后端此时还没把刚发送的 `user_input` 落到 recent/history 接口，本地那条用户消息会被历史同步直接冲掉；此外，`mergeRealtimeMessage(...)` 还会把连续两次相同文本的 user message 误判为重复。
- 处理动作：在 `apps/web/client/src/hooks/useTaskCreationAgent.ts` 中新增 `resolveExplicitAgentMessageKey(...)`、`isPendingLocalUserMessageForSession(...)`、`mergePendingLocalUserMessages(...)` 和 `messagesRef`，并把这层保护接入 `applyHistoryState(...)` 与 `loadHistory(...)`。
- 处理动作：同时收紧 `user_input / user_response` 的重复判断，只有在新旧两条消息都没有显式 `messageKey` 且文本完全一致时，才按重复处理。
- 处理动作：补充文档 `docs/agent研发文档/20260328_发送后用户消息偶发不可见_问题与修复方案.md`。
- 新进展：用户进一步反馈“skills 不够智能，做出来的 PPT/Word 千篇一律，不会根据内容决定怎么生成”，因此继续回到 Office 生成策略层排查，而不是只盯着联网和交付链路。
- 根因收敛：当前结果同质化不是单点问题，而是三层叠加造成的：`skill-attachment-templates.ts` 仍偏通用 brief、`altus-managed-prompt-service.ts` 为稳定性交付做了明显收紧、执行层只有通用工具没有内容决策契约。
- 处理动作：按仓库要求先补设计文档，不直接改业务逻辑；新增 `docs/agent研发文档/20260328_内容感知型Office生成设计.md`。
- 设计结论：最短正确路径不是继续把 skill 写得更长，而是在 `PPT / Word` 生成前强制引入一轮“内容原型识别 -> 结构策略 -> 风格策略 -> 最终生成”的内容感知 brief。
- 设计约束：本轮不新增 connector、不新增 UI、不更换生成引擎，只在 `skill + prompt` 两层落地内容感知契约，避免过度设计。
- 处理动作：同步在 `docs/agent研发文档/20260328_Office技能联网联动扩展_设计与实现.md` 中补充后续演进说明。
- 计划如何解决：等待用户确认设计文档后，再进入实现阶段，重点先改 `apps/web/client/src/lib/skill-attachment-templates.ts` 和 `apps/api/src/services/altus-managed-prompt-service.ts`。
- �½�չ���û�ȷ�Ͽ�ʼʵ�֡����ݸ�֪�� Office ���ɡ�����˴���ƽ׶ν��������ؽ׶Σ������ϸ񱣳����·������ȥ���� UI��connector ������㡣
- ������������������ `apps/web/client/src/lib/skill-attachment-templates.ts` �� `apps/api/src/services/altus-managed-prompt-service.ts` �ĵ�ǰʵ�֣�ȷ��ֻ��Ҫ�� `PPT/Word` skills �� Altus prompt �����Ƚ������ݾ�����Լ��
- ������������д `skill-attachment-templates.ts`������ԭ�� 6 ������ skills������ `office-ppt` ����Ϊ����ʶ�� content_archetype�����γ� generation_brief����ѡ slide_type �� style_pack�������ݸ�֪�档
- ����������ͬ���� `office-docx` ����Ϊ���ݸ�֪�棬���� `business_plan / formal_report / proposal / policy_process / meeting_memo / research_brief / external_statement` ������������ԭ�͡�
- ������������ `altus-managed-prompt-service.ts` �в��� Office ר�������ȷ `PPT / DOCX` ��������ǰ������ѡΨһ `contentArchetype`��bounded style pack ���ڲ����� brief����ֹ�����Ѳ�ͬ���ⶼд��ͬһ��Ĭ�Ͻṹ��
- �½�չ��`pnpm --filter web check` ��ͨ����˵��ǰ�� skill ģ���Ķ�δ���� TS �������⡣
- ����������ͨ���������ȷ�� `content_archetype / generation_brief / style_pack / contentArchetype` ����ʵ�������������ļ���
- ��������������ʵ���ĵ� `docs/agent�з��ĵ�/20260328_���ݸ�֪��Office����_ʵ������֤.md`������д����ĵ�״̬�������ĵ�ͣ���ڡ�ֻ���δ��ء���
- �ƻ���ν������һ���������û�ֱ���� `ְҵ�滮 PPT / ��ҵ�ƻ��� DOCX / �ڲ���ѵ PPT` �������Բ�ͬ���⣬�۲�ṹ�ͷ���Ƿ��Ѿ����������Թ������ƣ��ټ������� archetype ��ҳ��/�½ڵ�ӳ�䣬����������Ķ���Χ��
- 新进展：用户继续提出“把 PPT/Word/Excel 都升级成正式的办公模式”，因此当前任务从“内容感知优化”继续上升到“产品层模式升级”。
- 根因收敛：当前 Office 相关能力虽然已经能联网、能交付、能下载，但仍然主要依附在 `skill-attachment-templates.ts` 中，本质仍是 office skill，而不是正式 mode。
- 处理动作：重新梳理前端入口 `AttachmentPickerButton.tsx -> task-attachments.ts -> Home.tsx -> sendChatInput(metadata)` 与后端 prompt 承接链路，确认最短正确路径不是继续堆 skill 文案，而是让 Office 选择本身变成结构化 mode 输入。
- 处理动作：新增设计文档 `docs/agent研发文档/20260328_Office正式任务模式设计.md`，明确下一阶段会把 `PPT / Word / Excel` 从 generic skill 菜单中移出，升级为 first-class office mode。
- 设计结论：正式模式的最短实现路径是新增共享 `office mode` 定义层、前端新增“办公模式”入口、消息 metadata 传 `officeMode` 结构化对象、后端按 mode 生成 office execution contract、completion 按 mode-specific gate 校验交付。
- 处理动作：同步更新 `docs/agent研发文档/20260328_内容感知型Office生成设计.md`，标记其后续演进方向已切换为 `Office 正式任务模式`。
- 计划如何解决：等待用户确认这份“Office 正式任务模式”设计文档，通过后再开始实现，不提前修改业务代码，避免违反仓库协作规则。
- 新进展：用户明确提出“先把 PPT 相关的 skills 弄好，让它们协同工作”，并要求继续高精度借鉴 MiniMax `pptx-generator`，因此当前任务从“Office 正式模式总设计”收敛到“PPT 优先落地切片设计”。
- 处理动作：重新对照 MiniMax 参考资料 `README / pptx-generator/SKILL.md / references/slide-types.md / references/design-system.md / references/pitfalls.md`，重点提炼其多阶段工作流、slide type library、design system 和 QA gate，而不是只看表层 prompt 文案。
- 根因收敛：当前项目 PPT 之所以仍容易同质化，不是单点缺联网或缺图片，而是 `PPT 办公` 仍然是单体 skill brief，缺少“内容判型 -> 检索补材 -> 故事板 -> 视觉系统 -> 文件生成 -> QA”这条显式协同链路。
- 处理动作：新增设计文档 `docs/agent研发文档/20260328_PPT多阶段协同技能设计.md`，把本项目的最短实现路径定义为“一个 PPT 主技能 + 六个内部阶段子技能协同”，并明确每个阶段的输入输出契约与退出条件。
- 处理动作：新文档定义了第一版要落地的 `pageType / contentSubtype / paletteKey / styleRecipe / fontPairing / QA gate`，同时明确继续沿用当前 `python-pptx` 技术栈，只借鉴 MiniMax 的工作流与设计资产思路，不重做底层生成引擎。
- 计划如何解决：等待用户确认这份 PPT 多阶段协同设计文档；确认后直接进入代码实现，优先修改 `skill-attachment-templates.ts`、`altus-managed-prompt-service.ts` 和共享 PPT 契约文件，并用三条真实 PPT smoke 验证差异化是否拉开。
- 新进展：用户确认开始实现 `PPT 办公` 的多阶段协同方案，因此当前任务从“PPT 切片设计”进入“PPT 切片首版落地”。
- 处理动作：在 `packages/shared/src/ppt-generation.ts` 中新增共享 PPT 契约，抽出阶段、内容原型、页面类型、内容子类型、视觉系统和 QA gate 常量，并通过 `packages/shared/src/index.ts` 导出。
- 处理动作：重写 `apps/web/client/src/lib/skill-attachment-templates.ts`，把 `PPT 办公` 从单体 brief 升级成显式六阶段协同协议，同时保留现有 `Word / Excel` 能力不回退。
- 处理动作：在 `apps/api/src/services/altus-managed-prompt-service.ts` 中落地对应的阶段执行规则，要求 Altus 先形成 `PptGenerationBrief`、再做 `storyboard + visual system`、最后写 `presentation_manifest.json` 并按 QA gate 收尾。
- 处理动作：在 `apps/api/tests/altus-managed-prompt-service.test.ts` 中新增单测，验证多阶段协同协议已真实进入系统 prompt。
- 根因收敛：当前仓库的 `@oneceo/shared` 运行时导入策略还不适合 API 直接读取新增运行时常量，因此本轮采取“共享契约文件已抽出，API prompt 服务暂用本地镜像常量”的最短正确实现，先保证功能落地稳定。
- 处理动作：新增实现文档 `docs/agent研发文档/20260328_PPT多阶段协同技能实现与验证.md`，并回写 `docs/agent研发文档/20260328_PPT多阶段协同技能设计.md` 的状态。
- 新进展：最小验证已通过，`shared build`、`web check` 和 `api` prompt 单测全部成功。
- 计划如何解决：下一步如果继续推进，就开始跑三条真实 PPT smoke，验证职业规划、商业路演、内部培训三类 archetype 的结构和视觉是否真正拉开，而不是继续只看 prompt 文案。
- 新进展：为验证 `PPT 办公` 多阶段协同协议不是只停留在 prompt 层，继续跑了两条真实 PPT smoke，而不是只依赖单测。
- 处理动作：使用 `apps/api/scripts/_tmp/_tmp_altus_managed_office_smoke.ts` 触发 `office-ppt` 真链路，第一条是职业规划主题，最终 run `71b65460-f77e-4263-9a61-0b57ecef2f59` 成功 `completed`，交付 `career-plan-multiphase-smoke.pptx`，大小 `312868 bytes`。
- 处理动作：从第一条 smoke 的 stream tail 确认使用了 `web_search / web_extract / shell_execute / write_file / complete_task`，且最终 deck 包含封面图、两页视觉内容页和来源 URL。
- 处理动作：继续补跑第二条不同 archetype 的 smoke，主题改为 AI 学习助手创业路演，最终 run `503d4cf3-ebb4-41c4-82ad-5bfae923d730` 成功 `completed`，交付 `ai-learning-assistant-pitch-smoke.pptx`，大小 `45572 bytes`。
- 根因收敛：两条任务的结构已经明显分流，职业规划走过程/框架/目标型结构，创业路演走 problem / solution / market / business model / team / ask 结构，说明这轮多阶段协同规则已经开始对结果产生真实影响，而不只是停留在说明文案层。
- 处理动作：两次 smoke 产生的 sandbox `ir7e83mvvh7cgztz2n0f3` 和 `ibh466p9wxxipittwe5vh` 已关闭，两个测试 session 已删除，未留下额外计费资源。
- 处理动作：把两条真实 smoke 的 run id、产物大小、结构差异和清理情况回写到 `docs/agent研发文档/20260328_PPT多阶段协同技能实现与验证.md`。
- 计划如何解决：下一步如果继续提升 PPT 丰富度，就该从“协议层已生效”往“真实 builder 强制写 `presentation_manifest.json`、进一步提高视觉素材和图表质量”推进，而不是再回去单纯加长 skill 文案。

- 新进展：用户要求“再给 Word、Excel 也添加这样的 skills”，因此当前任务从 PPT 多阶段协同扩展到 Word / Excel 两条 Office 主链路。
- 处理动作：继续遵守仓库规则，未直接写业务代码，先重新对照 MiniMax minimax-docx 与 minimax-xlsx 的技能说明和参考文档，收敛其核心可复刻能力：任务路由、结构规划、风格系统、Formula-First/Validation gate 等。
- 根因收敛：当前 Word / Excel 虽然已具备联网与交付闭环，但仍停留在“增强版 skill brief”阶段，缺少像 PPT 一样的显式阶段协同协议，因此生成策略还不够智能和差异化。
- 处理动作：新增设计文档 docs/agent研发文档/20260328_WordExcel多阶段协同技能设计.md，把 Word 文档 定义为 docx_task_router / docx_research_curator / docx_outline_architect / docx_style_system_designer / docx_builder / docx_qa_reviewer 六阶段，把 Excel 表格 定义为 xlsx_task_router / xlsx_source_curator / xlsx_workbook_designer / xlsx_formula_planner / xlsx_builder / xlsx_qa_reviewer 六阶段。
- 处理动作：同步回写 docs/agent研发文档/20260328_Office正式任务模式设计.md，标记当前后续切片已经切到 Word / Excel 多阶段协同，但仍保持“先 skill+prompt 落地，暂不扩到 Office mode UI”的最短路径。
- 计划如何解决：等待用户审核新设计文档；如设计通过，下一步直接进入实现，优先改 pps/web/client/src/lib/skill-attachment-templates.ts、pps/api/src/services/altus-managed-prompt-service.ts 和必要的 shared 契约文件，然后再做 Word/Excel 真链路 smoke，并确保每次 E2B 测试后关闭 sandbox。
